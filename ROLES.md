# 역할 운영 정본

이 프로젝트의 역할은 AI 제품명이나 실행 도구명이 아니라 **현재 요청에서 부여된 책임**으로 결정한다. `CLAUDE.md`와 `GPT.md`라는 파일명은 기존 참조와 도구 호환을 위해 유지하며, 각각 작업자 규약과 검증자 규약을 뜻한다.

## 작업자

- 구현·수정·문서 반영과 `fix_log/` 상태 전이를 담당한다.
- 검증자가 기록한 발견을 재현하고 `확인`·`수정완료`·`기각`·`보류`·`이관`으로 처리한다.
- 독립 검증 결과를 자신이 `신규`로 작성하지 않는다.
- 작업 규약은 `CLAUDE.md`, 단계별 범위와 출구는 `project_docs/dev/개발_착수_지시서.md`를 따른다.

## 검증자

- 개발하지 않는다. 코드·설계·표준을 독립적으로 대조하고 가능한 검사를 직접 실행한다.
- 반례와 오류를 `fix_log/`에 `신규`로만 기록한다.
- 기존 항목의 상태를 바꾸거나 행을 삭제하지 않는다.
- 검증 규약은 `GPT.md`, 기록 규약은 `fix_log/README.md`를 따른다.

## 역할 분리

- 동일 작업 흐름에서 한 AI가 작업자와 검증자 역할을 동시에 수행하지 않는다.
- 사용자가 역할을 명시하면 그 역할만 수행한다. 역할이 불명확하면 변경 전에 사용자에게 확인한다.
- 실행 환경의 기능은 제품명으로 가정하지 않는다. 저장소 읽기, 명령 실행, 이미지 해석, 브라우저 렌더링, 하드웨어 접근 가능 여부를 각각 실제로 확인한다.
- 사용할 AI나 도구가 바뀌어도 역할별 권한·금지·산출물은 바뀌지 않는다.

## 호환 파일

| 파일 | 역할 | 파일명 정책 |
|---|---|---|
| `ROLES.md` | 역할 선택과 분리의 정본 | 제품 중립 이름 |
| `CLAUDE.md` | 작업자가 따르는 프로젝트 규약 | 기존 조항 참조 호환을 위해 유지 |
| `GPT.md` | 검증자가 따르는 검증 규약 | 기존 조항 참조 호환을 위해 유지 |

