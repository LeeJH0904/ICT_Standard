# 발견 사항 인덱스

> 기록 규약: `fix_log/README.md`
> **모든 발견은 이 표에 한 줄로 존재한다.** 개별 파일은 그 줄의 상세다.
> 상태 전이는 Claude만 수행한다. GPT는 `신규`로만 추가한다.
> **불변식**: `표준결함` 행 수 = `CLAUDE.md` §3.6 총계 = 명세서의 장애 지점 합계
> **자동 검사**: `python fix_log/meta_verify.py` — 인덱스·상세·설계 문서 수치를 기계 대조한다 (F-043)

**다음 ID: `F-233`**

## 표준결함 (19건) — 고칠 수 없음. 기획서 자산

| ID | 심각도 | 대상 | 요약 | 상태 | 상세 |
|---|---|---|---|---|---|
| F-001 | 치명 | 0943 표 7-4 | `NOTI_ERROR`와 `NOTI_DEVICE_VALUE`에 동일 코드 0x0800 할당 | 이관 | SIAP §6-1 |
| F-002 | 오류 | 0943 표 7-4 | Notify 코드가 0x0803까지인데 RESERVED 시작이 0x0805 (0x0804 공백) | 이관 | SIAP §6-2 |
| F-003 | 오류 | 0943 전체 | 엔디안 규정 부재 | 이관 | SIAP §6-3 |
| F-004 | 오류 | 0943 표 7-14 | `Value Type = FLOAT`의 표현 방식 미명시 | 이관 | SIAP §6-4 |
| F-005 | 오류 | 0943 7장 | 가변 요소 개수 N의 전달 방법 미규정 | 이관 | SIAP §6-5 |
| F-006 | 제안 | 0943 표 7-10 | `SUCESS` 오타 | 이관 | SIAP §6-6 |
| F-007 | 제안 | 0943 8.2.1.4 | 절 제목이 8.2.1.3과 중복 | 이관 | SIAP §6-7 |
| F-008 | 제안 | 0943 8.2.1.2 | 그림 8-51 설명의 메시지명 오기 | 이관 | SIAP §6-8 |
| F-009 | 제안 | 0943 8장 | 그림 캡션 13개가 본문과 분리 | 이관 | SIAP §6-9 |
| F-010 | 오류 | 1369-P1 그림 7-2 / 7-4 | 엔티티 속성 구성 불일치 | 이관 | DB §5-1 |
| F-011 | 오류 | 1369-P1 6.3.3.1 / 그림 7-3 | 환경상태 항목 수 불일치 (본문 10종, 그림 9종 — 풍속 누락) | 이관 | DB §5-2 |
| F-012 | 오류 | 1369-P1 6.3.4.1 | 장치상태 항목 수 불일치 (본문 7종에 차광막 포함, 절·그림은 6종) | 이관 | DB §5-3 |
| F-034 | 오류 | 0943 표 7-15 | `DEVICE_PROPERTY`의 USER DEPENDENT 5필드 타입 선택 규칙 미규정 | 이관 | SIAP §6-5a |
| F-035 | 오류 | 0943 표 7-18 | `MSG_CONTROL_PROFILE` 시간 3필드의 단위 미규정 (`Period`만 sec 명시) | 이관 | SIAP §6-5b |
| F-036 | 오류 | 1369-P1 6.2.3 / 그림 7-2·7-4 | 생육작물이 본문에만 있고 그림에는 없음 | 이관 | DB §5-4 |
| F-037 | 오류 | 1369-P1 6.3.3.8 / 그림 7-3 | 감우의 단위 취급이 본문과 그림에서 상반됨 | 이관 | DB §5-5 |
| F-038 | 오류 | 1369-P1 7.1(2) / 7.2.2 | 농장관리 관계가 개념모델에만 있고 논리모델에 없음 | 이관 | DB §5-6 |
| F-065 | 오류 | 0943 표 7-18 / 표 7-13 | `Message Receive Timeout` 과 가변 요소 개수 N(최대 255)의 관계 미규정 — 기본 프로파일로 최대 프레임을 수용할 수 없다 | 이관 | SIAP §6-5c |
| F-069 | 치명 | 0943 표 1-1 / 7장 | 프레임 경계 구분자·무결성 검사(CRC) 미규정 — 통신 환경에 RS232/485(순수 바이트 스트림)를 명시하면서 프레임 시작을 찾을 수단을 두지 않았다 | 이관 | SIAP §6-10 |

**0943 13건 + 1369-P1 6건 = 19건**

## 코드버그 · 문서불일치 · 요건위반

| ID | 심각도 | 분류 | 대상 | 요약 | 상태 | 상세 |
|---|---|---|---|---|---|---|
| F-013 | 오류 | 코드버그 | `spec_verify.py` · `test_contract.py` | 인코딩·경로 미지정으로 CP949 환경에서 검증 스크립트 실패 | 수정완료 | F-013_windows_json_encoding.md |
| F-014 | 오류 | 코드버그 | `contracts/frame.py` | `Header.trans_type`이 `TransType`이라 미정의값 0x03을 담을 수 없음 | 수정완료 | F-014_invalid_trans_type_contract.md |
| F-015 | 오류 | 코드버그 | `db/schema.sql` | 1:N 관계의 단일 부모 카디널리티 미강제 (8건) | 수정완료 | F-015_db_cardinality_not_enforced.md |
| F-016 | 오류 | 코드버그 | `db/schema.sql` · `verify.py` | 표준 유래 불변성 제약 11건 누락 | 수정완료 | F-016_db_immutability_incomplete.md |
| F-017 | 위험 | 문서불일치 | DB 설계서 §4.3 · `schema.sql` | 승인 게이트의 DB 보장 범위 과장, 실행 테이블 우회 가능 | 수정완료 | F-017_approval_gate_bypass.md |
| F-022 | 위험 | 문서불일치 | `spec_verify.py` · `frame.py` · SIAP 명세서 | USER DEPENDENT 필드를 근거 없이 FLOAT로 고정 | 수정완료 | F-022_user_dependent_forced_float.md |
| F-023 | 오류 | 문서불일치 | `fix_log/` · `CLAUDE.md` | 표준결함 총계가 문서마다 불일치 (행 삭제 주장은 사실 아님 — 처리 기록 참조) | 수정완료 | F-023_fix_log_history_integrity.md |
| F-024 | 오류 | 코드버그 | `db/verify.py` | 상대 경로 의존 + 모든 예외를 제약 통과로 오판정 | 수정완료 | F-024_db_verifier_false_positive.md |
| F-025 | 오류 | 코드버그 | `contracts/siap_iface.py` | 절대 import로 패키지 경로에서 로드 실패 | 수정완료 | F-025_contract_package_import.md |
| F-026 | 오류 | 코드버그 | `siap/spec_verify.py` | `value_type` 중복 인자로 main과 어긋날 수 있음 | 수정완료 | F-026_user_dependent_pack_mismatch.md |
| F-027 | 치명 | 문서불일치 | 아키텍처 §3.1 | **수신 Request·Notify에 대한 회신 경로 부재** — 기능 1 성립 불가 | 수정완료 | F-027_inbound_reply_path_missing.md |
| F-028 | 오류 | 문서불일치 | 아키텍처 §5.3 | `spy://` 출력 형식 ≠ replay 입력, epoch 시각으로 sleep 계산 | 수정완료 | F-028_replay_capture_contract.md |
| F-029 | 위험 | 문서불일치 | 아키텍처 §8 | 전이 의존성의 플랫폼별 바이너리 휠 누락 | 수정완료 | F-029_offline_wheel_assumption.md |
| F-030 | 오류 | 문서불일치 | `db/schema.sql` · 아키텍처 | 승인된 명령과 실제 실행 명령이 결속되지 않음 | 수정완료 | F-030_approval_command_unbound.md |
| F-031 | 오류 | 문서불일치 | `siap_iface.py` · Frame 명세서 | "Frame 하나뿐" 원칙 서술이 실제 계약과 불일치 | 수정완료 | F-031_frame_only_boundary_broken.md |
| F-032 | 오류 | 문서불일치 | `db/schema.sql` · DB 설계서 | 표준 커버리지 공백 3건 (생육작물·감우 단위·농장관리) | 수정완료 | F-032_db_standard_coverage_gaps.md |
| F-033 | 위험 | 문서불일치 | SIAP 명세서 · 아키텍처 | `MSG_CONTROL_PROFILE` 시간 단위 미결정 | 수정완료 | F-033_profile_time_unit_undefined.md |
| F-039 | 치명 | 코드버그 | `db/schema.sql` · `verify.py` | 승인 action이 NULL이면 임의 명령 실행 가능하고 승인 조건도 사후 변조 가능 | 수정완료 | F-039_approval_snapshot_bypass.md |
| F-040 | 오류 | 문서불일치 | 아키텍처 §3.1-a · `siap_iface.py` | 즉시 회신 설계가 메시지 전량과 FrameBuilder 계약을 충족하지 못함 | 수정완료 | F-040_inbound_dispatch_incomplete.md |
| F-041 | 오류 | 문서불일치 | 아키텍처 §3.1-a·§4.2 | I/O 루프가 무수신 상태에서 송신 큐를 처리하지 못하고 Response 매칭 경로도 없음 | 수정완료 | F-041_io_loop_liveness.md |
| F-042 | 오류 | 문서불일치 | 아키텍처 §5.3~5.4 | replay가 `dir`을 무시해 기록된 TX 프레임까지 RX로 재주입 | 수정완료 | F-042_replay_direction_ignored.md |
| F-043 | 오류 | 문서불일치 | `fix_log/` · `CLAUDE.md` · 아키텍처 | 상태·심각도·총계·트리거 수·회귀 테스트 기록이 실제 파일과 불일치 | 수정완료 | F-043_verification_metadata_drift.md |
| F-044 | 오류 | 코드버그 | `siap/spec_verify.py` | 32비트 INT/UINT 범위를 검사하지 않고 비트 마스킹으로 조용히 래핑 | 수정완료 | F-044_integer_value_wraparound.md |
| F-045 | 오류 | 코드버그 | `siap/spec_verify.py` | 출력의 em dash 때문에 한국어 Windows 기본 콘솔(CP949)에서 검증기가 중단 | 수정완료 | F-045_spec_verify_cp949_regression.md |
| F-046 | 오류 | 문서불일치 | 아키텍처 §3.1-b · `frame.py` | 응답 매칭이 `Message Type`을 보지 않아 다른 요청의 Response·ACK가 소비됨 | 수정완료 | F-046_response_kind_not_matched.md |
| F-047 | 오류 | 코드버그 | `siap/spec_verify.py` | 디코딩이 Reserved·미정의 `Value Type`을 정상값으로 수용 | 수정완료 | F-047_unpack_value_type_not_validated.md |
| F-048 | 오류 | 코드버그 | `db/schema.sql` | 승인 후 승인자·승인시각을 위조할 수 있음 | 수정완료 | F-048_approval_provenance_mutable.md |
| F-049 | **치명** | 코드버그 | `db/schema.sql` | 승인 명령은 유지한 채 **다른 구동기**를 작동시킬 수 있음 | 수정완료 | F-049_approved_target_unbound.md |
| F-050 | 오류 | 코드버그 | `fix_log/meta_verify.py` | 자식 출력 디코딩 실패를 PASS로 처리 — F-045 검사가 실제로는 무효 | 수정완료 | F-050_meta_verify_decode_false_pass.md |
| F-051 | 오류 | 문서불일치 | `api/openapi.json` · API 명세서 | `ControlAction`이 열려 있어 금지한 대상 장치를 실을 수 있음 | 수정완료 | F-051_control_action_allows_extra_target.md |
| F-052 | 위험 | 문서불일치 | API 명세서 §2.1 · `openapi.json` | 승인자 식별이 자체 주장 헤더 — 신원 보장 아님을 명시 (인가 제약은 보류) | 수정완료 | F-052_self_asserted_approver_identity.md |
| F-053 | 오류 | 문서불일치 | API 명세서 · 아키텍처 §4.4 | API 쓰기 4건이 "I/O 스레드 단독 쓰기" 원칙과 충돌 — 규칙 저장 경로 부재 | 수정완료 | F-053_api_write_ownership_conflict.md |
| F-054 | 오류 | 문서불일치 | `api/openapi.json` · `api_verify.py` | API가 INT·UINT 32비트 값 범위를 강제하지 않음 | 수정완료 | F-054_control_value_range_unconstrained.md |
| F-055 | 오류 | 문서불일치 | `api/openapi.json` · `siap/spec_verify.py` | FLOAT 범위 미강제 — 반례 집합에 FLOAT가 빠져 F-054 수정이 절반 | 수정완료 | F-055_float32_range_unconstrained.md |
| F-056 | 오류 | 코드버그 | `api/api_verify.py` | 쓰기 경로 검사가 개수만 세어 경로 교체를 거짓 통과 | 수정완료 | F-056_api_write_set_false_positive.md |
| F-057 | 오류 | 문서불일치 | 아키텍처 §4.4-a · `db/schema.sql` | 쓰기 소유권 표에서 테이블 9개 누락 — 불변식이 닫히지 않음 | 수정완료 | F-057_db_write_ownership_incomplete.md |
| F-058 | 오류 | 코드버그 | `siap/spec_verify.py` | FLOAT·정수 변환 단계의 원시 예외 누출 — 단일 예외 계약이 깨짐 | 수정완료 | F-058_float_conversion_error_leak.md |
| F-059 | 오류 | 코드버그 | `fix_log/meta_verify.py` | 소유권 중복 배정을 통과 — "정확히 하나"의 절반만 검사 | 수정완료 | F-059_ownership_duplicate_false_pass.md |
| F-060 | 오류 | 문서불일치 | 골든 벡터 X08 · 골든벡터 명세서 | 정상 NEC 알림을 위반으로 표시 — 격리되어 alert·ACK 경로가 막힘 | 수정완료 | F-060_valid_error_notify_quarantined.md |
| F-061 | 오류 | 문서불일치 | `golden_ext.jsonl` E03·E04 | extended 벡터가 양방향 알림을 N→G로 축소 | 수정완료 | F-061_extended_notify_direction_wrong.md |
| F-062 | 오류 | 코드버그 | `golden_verify.py` | 주입별 기대 오류가 뒤바뀌어도 통과 — 집합만 비교 | 수정완료 | F-062_violation_mapping_false_pass.md |
| F-063 | 오류 | 코드버그 | `golden_layout.py` · `spec_verify.py` · `meta_verify.py` | 생성기가 검증 중 산출물을 덮어써 드리프트를 마스킹 | 수정완료 | F-063_golden_source_drift_masked.md |
| F-064 | 오류 | 문서불일치 | 아키텍처 §6.2 · 골든 B03 | 최대 프레임을 51byte로 가정해 타임아웃 근거가 틀림 | 수정완료 | F-064_max_frame_timeout_underestimated.md |
| F-066 | 오류 | 코드버그 | `siap/spec_examples.json` | 재생성한 산출물을 커밋에 넣지 않아 `--check` 실패 | 수정완료 | F-066_spec_examples_stale_newline.md |
| F-067 | 오류 | 문서불일치 | SIAP 명세서 §7 | 폐기된 "최대 51byte" 타임아웃 근거가 남아 문서 간 모순 | 수정완료 | F-067_stale_timeout_rationale.md |
| F-068 | 오류 | 코드버그 | `golden_layout.py` · `spec_verify.py` | `Num. of Devices` 와 `DEVICE_PROPERTY` 개수 불일치 (5건) | 수정완료 | F-068_b03_device_count_mismatch.md |
| F-070 | 오류 | 요건위반 | `CLAUDE.md` §9 · `진행보고서.md` | 저장소 문서에 개인 경로가 남아 있음 (§1-4 블라인드 심사 위반). `meta_verify` 스캔이 `.md` 제외 + 경로 패턴 부재로 사각지대였다 | 수정완료 | F-070_personal_path_in_repo_docs.md |
| F-071 | 오류 | 문서불일치 | 펌웨어 설계서 §5.9·§6.2 | Notify의 ACK·재전송 생명주기가 상태 머신에 없고 리부팅은 송신 완료만으로 리셋 | 수정완료 | F-071_notify_ack_lifecycle_missing.md |
| F-072 | 오류 | 문서불일치 | 펌웨어 설계서 §6.1·§6.2 | 부팅 진단 실패 후 FAULT 복구가 연결 승인 없이 RUNNING으로 전이 | 수정완료 | F-072_fault_recovery_skips_connection.md |
| F-073 | 오류 | 문서불일치 | 펌웨어 설계서 §0·§2.2·§3.4·§3.5 | 재시도·포인터 메모리·Subtype 수치가 실제 표·계약과 불일치 | 수정완료 | F-073_firmware_numeric_drift.md |
| F-074 | 오류 | 코드버그 | `firmware/firmware_verify.py` | 필수 ACK 전이 삭제와 수치 변조를 26/26으로 거짓 통과 | 수정완료 | F-074_firmware_verifier_false_pass.md |
| F-075 | 위험 | 문서불일치 | 펌웨어 설계서 §4.1 | `bp_write`가 비트폭 초과를 반환·강제하지 못해 호출자 누락 시 조용한 절단 가능 | 수정완료 | F-075_bitpack_range_contract_open.md |
| F-076 | 오류 | 문서불일치 | `CLAUDE.md` §3.5 · 펌웨어 설계서 §6.2·§6.5·§9 | 연결 영구 실패가 `FAULT`·`HALTED`로 이중 결정되고 `HALTED`도 상태 무관 전이로 탈출 가능 | 수정완료 | F-076_halted_policy_conflict.md |
| F-077 | 오류 | 문서불일치 | 펌웨어 설계서 §6.2·§6.3·§6.4 | 같은 주기의 Device Value·Keep Alive가 단일 pending 슬롯에서 영구 기아 가능 | 수정완료 | F-077_periodic_notify_starvation.md |
| F-078 | 오류 | 문서불일치 | `CLAUDE.md` §4 · 펌웨어 설계서 §4.1·§4.3 | F-075의 bool 계약이 규약과 불일치하고 미사용 경고·20/32bit 범위식도 성립하지 않음 | 수정완료 | F-078_bitpack_contract_still_unsafe.md |
| F-079 | 오류 | 문서불일치 | `아키텍처_설계서.md` §3.1 | 노드 속성 설정 3종이 0937 6.2 DMS(공공데이터)로 잘못 배정됨 — 6.1 EMS 가 맞다 | 수정완료 | F-079_dms_service_misassignment.md |
| F-080 | 오류 | 코드버그 | `services/services_verify.py` | 0937 원문·아키텍처·근거 파일을 읽지 않아 요구 문구 교환·가짜 구현 근거·F-079 재발을 모두 거짓 통과 | 수정완료 | F-080_services_verifier_semantic_blindspot.md |
| F-081 | 오류 | 문서불일치 | `services/0937_요구사항_대조표.md` §1 | 자체 강도 규칙과 원문 종결어미가 7개 행에서 불일치하여 집계도 필수 16/능력 10/선택 7로 왜곡 | 수정완료 | F-081_0937_requirement_strength_misclassified.md |
| F-082 | 오류 | 문서불일치 | `services/0937_요구사항_대조표.md` §1·§4 | 필수·필수(능력) 6개 행을 충족으로 판정했지만 등록·삭제·모델 실행·구역 수집·수집 누락 알림 경로가 설계 근거에 없음 | 수정완료 | F-082_0937_full_verdict_evidence_gaps.md |
| F-083 | 치명 | 문서불일치 | `web/화면_설계서.md` §6 · `api/openapi.json` | 기능 3의 AI 초안 생성·거부 흐름이 HTTP 계약에 없음 — `createRuleDraft`는 클라이언트가 보낸 문구를 저장할 뿐 `mms.run_model`을 호출하지 않음 | 수정완료 | F-083_rules_workflow_api_disconnected.md |
| F-084 | 치명 | 문서불일치 | `web/화면_설계서.md` §5.3 · `api/openapi.json` | 브라우저의 위반 주입 버튼이 로컬 `sim/inject.py`를 호출할 API·IPC가 없어 기능 2 핵심 시연을 실행할 수 없음 | 수정완료 | F-084_web_injection_path_missing.md |
| F-085 | 오류 | 문서불일치 | `web/화면_설계서.md` §5 · `api/openapi.json` `Frame` | 비트 오프셋·필드 분해·판정 종류가 응답 계약에 없어 화면 직접 디코딩 금지와 검증 뷰 요구를 동시에 만족할 수 없음 | 수정완료 | F-085_frame_view_contract_incomplete.md |
| F-086 | 오류 | 문서불일치 | `api/openapi.json` `setDeviceProperty` · `contracts/siap_iface.py` | 설정 API가 0943 `REQ_SET_DEVICE_PROPERTY` 송신을 약속하지만 경계 `FrameBuilder`에 해당 요청 빌더가 없음 | 수정완료 | F-086_device_property_builder_missing.md |
| F-087 | 오류 | 문서불일치 | `services/0937_요구사항_대조표.md` A.1-5 · `web/화면_설계서.md` §7 | 임계값 프레임 전송만으로 알림 발송 시간·클라우드→사용자 알림까지 충족했다고 과대 판정 | 수정완료 | F-087_alert_requirement_overclaimed.md |
| F-088 | 오류 | 문서불일치 | `api/openapi.json` `DeviceProperty*` | 새 설정 요청이 빈 선택자·상충 선택자·빈 변경·32bit 범위 밖 임계값을 스키마상 허용 | 수정완료 | F-088_device_property_validation_open.md |
| F-089 | 오류 | 코드버그 | `web/web_verify.py` | 경로 수·필드명·파일 구조·핵심 화면 데이터/호출 경계 오류가 현재 존재해도 40/40으로 거짓 통과 | 수정완료 | F-089_web_verifier_false_green.md |
| F-090 | 오류 | 문서불일치 | `진행보고서.md` · `API_명세서.md` · 아키텍처·0937 대조표 | 설계 완료 후에도 단계·표준결함·API 오퍼레이션·쓰기·검증 수치가 이전 상태로 남아 문서 정본이 분기됨 | 수정완료 | F-090_design_docs_completion_drift.md |
| F-091 | 오류 | 문서불일치 | `api/openapi.json` `Rule*` · `db/schema.sql` `control_rule` · 웹 설계서 §6 | F-083 보완 뒤에도 빈 모델/초안·생성경로 위조·거부사유 누락/변조가 계약상 허용되고 응답이 증거 필드를 생략할 수 있음 | 수정완료 | F-091_rule_provenance_rejection_invariants_open.md |
| F-092 | 오류 | 문서불일치 | `api/openapi.json` `Alert` · `db/schema.sql` `alert` | F-085가 요구한 NEC 알림→원본 프레임 결속이 선택 필드에 그쳐 알림 응답·DB 행이 `frame_id` 없이도 유효 | 수정완료 | F-092_nec_alert_frame_link_optional.md |
| F-093 | 오류 | 문서불일치 | `api/openapi.json` `DeviceProperty*` | F-088 처리 기록과 달리 null 선택자·위치 없는 구역 선택이 유효하고 전량 거부 422·타입 오류 계약이 없음 | 수정완료 | F-093_device_property_fix_incomplete.md |
| F-094 | 오류 | 문서불일치 | 웹 설계서 §9 · 0937 대조표 §4.5·§5 · API 명세서 §6 | F-087·F-090 뒤에도 A.1-5 판정·부속서 집계·쓰기 4→5·검증 76종 등 이전 수치와 문구가 남음 | 수정완료 | F-094_design_completion_drift_remains.md |
| F-095 | 오류 | 코드버그 | `api_verify.py` · `web_verify.py` · `services_verify.py` · `meta_verify.py` | F-091~F-094의 유효 반례와 문서 드리프트가 있는데도 전체 회귀 60/60·55/55·37/37·47/47로 거짓 통과 | 수정완료 | F-095_regression_blindspots_after_f090.md |
| F-096 | 오류 | 코드버그 | `project_docs/dev/dev_verify.py` | 하위 프로세스 stdout을 항상 UTF-8로 가정해 디코딩 — 파이프 연결 시 한국어 Windows 로케일(CP949)로 써서 한글이 깨지고 "N/N 통과" 매치가 조용히 실패 | 수정완료 | F-096_dev_verify_subprocess_cp949_decode.md |

| F-097 | 오류 | 코드버그 | `tools/offline_verify.py` | 잘못된 직접 의존성·`.gitignore`·`.git/` 패키징 정책을 거짓 통과 | 수정완료 | F-097_offline_verifier_policy_bypass.md |
| F-098 | 오류 | 코드버그 | `tools/where.py` | 단계 2c·7·8의 필수 출구 일부를 실행하지 않아 완료 단계를 과대 판정 | 수정완료 | F-098_where_exit_coverage_incomplete.md |
| F-099 | 위험 | 문서불일치 | 아키텍처 §8.3 · `project_code/requirements.txt` | 전이 의존성을 requirements에 고정한다는 문구가 직접 의존성 3개 정본·구현과 충돌 | 수정완료 | F-099_transitive_requirement_pin_conflict.md |
| F-100 | 오류 | 문서불일치 | `project_docs/dev/GPT_검증_프롬프트.md` | 기존 건수·마지막 ID 하드코딩이 신규 기록 즉시 낡아져 전체 회귀 실패 | 수정완료 | F-100_prompt_finding_count_hardcoded.md |
| F-101 | 오류 | 코드버그 | `spec_verify.py` · `run_all.py` | 기본 회귀가 spec_examples 드리프트를 덮어써 F-063 재발을 마스킹 | 수정완료 | F-101_spec_verify_default_overwrite.md |
| F-102 | 오류 | 코드버그 | `tools/offline_verify.py` | CP949 기본 콘솔에서 em dash 출력 때문에 단계 0 출구가 전 항목 OK 뒤 종료 코드 1 | 수정완료 | F-102_offline_verify_cp949_exit_failure.md |
| F-103 | 오류 | 코드버그 | `tools/offline_verify.py` | 버전 미고정·중복 버전 휠·gitignore 재포함을 함께 넣어도 5항목 전부 거짓 통과 | 수정완료 | F-103_offline_verifier_semantic_policy_bypass.md |
| F-104 | 오류 | 코드버그 | `tools/where.py` | `avr-size` 종료 코드만 보고 실측값을 설계 예산과 비교하지 않아 과대 바이너리를 통과 | 수정완료 | F-104_where_avr_size_budget_unchecked.md |
| F-105 | 오류 | 코드버그 | `tools/layer_verify.py` | 상대 import·동적 import로 같은 금지 계층 의존성을 만들어도 7/7 거짓 통과 | 수정완료 | F-105_layer_verify_import_bypass.md |
| F-106 | 오류 | 코드버그 | `project_code/contracts/test_contract.py` · `tools/layer_verify.py` | `FakeSiapLink`가 빈 클래스이거나 문법 오류여도 단계 1 출구 56/56·7/7 통과 | 수정완료 | F-106_fake_link_contract_unverified.md |
| F-107 | 오류 | 문서불일치 | Frame 구조 명세서 §5.1·§6·§7 | 현행 56/56·`set_device_property`·완료된 단계 1 산출물을 53/53·다음 단계로 잘못 기술 | 수정완료 | F-107_frame_spec_stage1_drift.md |
| F-108 | 오류 | 코드버그 | `fix_log/meta_verify.py` | 정상 offline 검증 시간이 고정 180초 제한과 맞닿아 CP949 회귀가 TimeoutExpired로 불안정 실패 | 수정완료 | F-108_meta_offline_cp949_timeout.md |
| F-109 | 오류 | 코드버그 | `tools/layer_verify.py` | 패키지 접두어 import·별칭 동적 import로 금지 계층을 참조해도 7/7 거짓 통과 | 수정완료 | F-109_layer_verify_qualified_alias_bypass.md |
| F-110 | 오류 | 코드버그 | `project_code/contracts/test_contract.py` | `send(timeout)`·`start(**opts)`가 없고 `recv()`가 iterator가 아닌 Fake도 60/60 거짓 통과 | 수정완료 | F-110_fake_link_signature_contract_unchecked.md |
| F-111 | 오류 | 코드버그 | `project_code/firmware/tests/Makefile` | Windows 빌드가 금지 `test_bitpack.exe`를 남겨 전체 회귀·현재 단계 판정을 실패시킴 | 수정완료 | F-111_bitpack_windows_executable_artifact.md |
| F-112 | 오류 | 코드버그 | `project_code/firmware/tests/test_bitpack.c` | 필수 9bit 경계·버퍼 불변 반례가 모두 34/34 거짓 통과 | 수정완료 | F-112_bitpack_required_cases_missing.md |
| F-113 | 오류 | 코드버그 | `project_code/firmware/tests/test_bitpack.c` · `Makefile` | `SIAP_WUR`를 제거해도 원본 테스트와 빌드가 34/34 통과 | 수정완료 | F-113_bitpack_wur_regression_unguarded.md |
| F-114 | 오류 | 코드버그 | `project_code/firmware/tests/check_wur.py` | 누락 소스·무관한 컴파일 오류도 `SIAP_WUR` 정상으로 거짓 통과 | 수정완료 | F-114_wur_compile_failure_false_pass.md |
| F-115 | 오류 | 코드버그 | `project_code/firmware/tests/test_bitpack.c` | 9bit write/read를 함께 LSB-first로 바꿔도 39/39 거짓 통과 | 수정완료 | F-115_bitpack_ninebit_order_blindspot.md |
| F-116 | 오류 | 코드버그 | `project_docs/contracts/vectors/golden_layout.py` · `golden_verify.py` | B02는 N=0 형식 오류라고 적고도 `normal`로 생성되어 C 골든 출구가 148/150 실패 | 수정완료 | F-116_b02_golden_judgement_mismatch.md |
| F-117 | 오류 | 코드버그 | `tools/where.py` | 단계 2a 정리가 2b 바이너리를 삭제하지만 2b는 재빌드하지 않아 구조적으로 통과 불가 | 수정완료 | F-117_where_stage2b_unbuildable.md |
| F-118 | 오류 | 코드버그 | `tools/core_purity_verify.py` | `#include/**/<Arduino.h>` 플랫폼 의존 코드를 4/4 거짓 통과 | 수정완료 | F-118_core_purity_include_comment_bypass.md |
| F-119 | 오류 | 코드버그 | `project_code/firmware/tests/test_golden.c` | 골든의 기대 `kind`·`n`을 비교하지 않아 메시지 종류 오매핑이 149/149 거짓 통과 | 수정완료 | F-119_golden_kind_n_unchecked.md |
| F-120 | 오류 | 코드버그 | `project_code/firmware/core/siap_frame.c` | 설계상 N=16 상한인데 N=17 프레임을 정상 종류로 수용 | 수정완료 | F-120_frame_device_limit_unenforced.md |
| F-121 | 오류 | 코드버그 | `tools/where.py` | `test_node_state`가 없는데 단계 2c를 통과해 현재 단계를 3으로 과대 판정 | 수정완료 | F-121_where_stage2c_missing_target.md |
| F-122 | 오류 | 코드버그 | `tools/core_purity_verify.py` | 간접 보드 매크로·매크로 include로 플랫폼 종속 `core/`가 6/6 거짓 통과 | 수정완료 | F-122_core_purity_macro_alias_bypass.md |
| F-123 | 오류 | 코드버그 | `project_code/firmware/core/siap_frame.c/.h` | 송신 put 함수가 51byte 윈도우 용량을 검사하지 않아 배열 밖 기록 | 수정완료 | F-123_tx_window_out_of_bounds.md |
| F-124 | 오류 | 문서불일치 | 단계 2b 지정 설계 문서·개발 착수 지시서 | F-120의 N=16 분기·53건 골든 변경이 지정 문서에 반영되지 않음 | 수정완료 | F-124_f120_design_docs_stale.md |
| F-125 | 치명 | 문서불일치 | `CLAUDE.md` §0·§3.6 · `docs/` | 핵심 주장 4의 근거 `docs/standard-findings.md`가 없는데 표준결함 19건은 이관 상태 | 수정완료 | F-125_standard_findings_not_migrated.md |
| F-126 | 오류 | 코드버그 | `project_code/firmware/core/siap_frame.c` | `Num. of Devices`와 실제 `DEVICE_PROPERTY` N이 달라도 SUCCESS 수용 | 수정완료 | F-126_combined_device_count_unchecked.md |
| F-127 | 오류 | 코드버그 | `project_code/firmware/core/siap_frame.c` | 표 7-10~7-15의 예약값 4종을 SUCCESS로 수용 | 수정완료 | F-127_reserved_field_values_accepted.md |
| F-128 | 오류 | 코드버그 | `tools/core_purity_verify.py` | 임의 이름 보드 매크로 분기가 순수성 검사 3겹을 모두 우회 | 수정완료 | F-128_core_purity_custom_macro_bypass.md |
| F-129 | 오류 | 문서불일치 | 펌웨어 설계서 §3.5·§5.3 · `siap_types.h` | PROGMEM SRAM 예산 주장과 일반 static const 구현이 모순 | 수정완료 | F-129_progmem_budget_implementation_mismatch.md |
| F-130 | 치명 | 코드버그 | `node_state.c` · 펌웨어 설계서 §6.3·§6.6 | 공통 최소 Period가 디바이스별 주기·Event 알림·Event-only 오류 감지를 무효화 | 수정완료 | F-130_device_period_event_semantics_broken.md |
| F-131 | 오류 | 코드버그 | `test_node_state.c` | RSC 9종 중 2종만 검사해 INVALID_GCG_ID 오분류 변이가 61/61 통과 | 수정완료 | F-131_node_state_rsc_matrix_untested.md |
| F-132 | 오류 | 코드버그 | `node_state.c` | ACK가 응답 Message Type을 확인하지 않고 연결 Request pending을 해제 | 수정완료 | F-132_ack_clears_request_pending.md |
| F-133 | 오류 | 코드버그 | `node_state.c` · `test_node_state.c` | UART 포화 시 미전송 헤더를 덮어써 NOTI_DEVICE_VALUE 프레임 유실 | 수정완료 | F-133_node_state_partial_write_loss.md |
| F-134 | 오류 | 문서불일치 | `CLAUDE.md` §3.5 · 펌웨어 설계서 §9 · `docs/standard-findings.md` | 표준 미규정 결정 9건의 이관 규칙이 표준결함 19건 전용 정본·메타 검증과 충돌 | 수정완료 | F-134_undefined_decision_migration_policy_conflict.md |
| F-135 | 치명 | 코드버그 | `siap/build.py` · `firmware/core/node_state.c` | Message Identifier가 0xFFFF 다음 0이 아니라 1로 돌아 표준 7.2.2를 위반 | 수정완료 | F-135_msg_id_zero_skipped.md |
| F-136 | 오류 | 코드버그 | `tools/xcodec_verify.py` | 골든 53건 전량 교차를 선언하지만 9건을 제외하고 44건만 C↔Python 바이트 대조 | 수정완료 | F-136_xcodec_skips_nine_vectors.md |
| F-137 | 치명 | 코드버그 | `siap/link.py` · `siap/registry.py` | 주입된 실제 처리기의 연결 성공은 registry를 갱신하지 않아 이후 프레임을 미등록으로 거부 | 수정완료 | F-137_injected_connection_not_registered.md |
| F-138 | 오류 | 코드버그 | `siap/link.py` | read 오류가 송신 큐 처리를 영구 건너뛰어 send()가 Timeout×(Retry+1) 상한을 초과 | 수정완료 | F-138_send_bound_bypassed_on_read_error.md |
| F-139 | 오류 | 코드버그 | `siap/link.py` | 부분 쓰기 뒤 0 반환 시 잔여 바이트를 버리고도 송신 성공으로 집계 | 수정완료 | F-139_link_partial_write_truncation.md |
| F-140 | 오류 | 코드버그 | `siap/codec.py` | 초기 헤더 위반에서 신뢰할 수 없는 payload_len만큼 삭제해 뒤의 정상 프레임까지 유실 | 수정완료 | F-140_decoder_resync_drops_valid_frame.md |
| F-141 | 오류 | 코드버그 | `firmware/core/siap_frame.c` | 위반 헤더의 거짓 payload_len만큼 미래 바이트를 drain해 바로 뒤 정상 프레임을 유실 | 수정완료 | F-141_c_decoder_drain_trusts_payload_len.md |
| F-142 | 오류 | 코드버그 | `tools/xcodec_verify.py` | C 덤프가 비정상 종료해도 stdout 건수만 맞으면 8/8 거짓 통과 | 수정완료 | F-142_xcodec_ignores_dump_exit.md |
| F-143 | 오류 | 코드버그 | `siap/tests/test_link.py` | 2초 send 상한 테스트가 4초까지 허용해 3초 위반 구현도 거짓 통과 | 수정완료 | F-143_send_bound_test_too_loose.md |
| F-144 | 오류 | 코드버그 | `tools/where.py` | 단계 0 검사가 "미구현" 문구를 영구 요구해 단계 4 완료 후 현재 단계가 0으로 후퇴 | 수정완료 | F-144_where_stage0_stage4_assertion_conflict.md |
| F-145 | 치명 | 코드버그 | `sim/virtual_node.py` · `sim/inject.py` | live 주입 X06~X08이 골든의 Node ID=3 미등록 때문에 목표 판정 대신 `INVALID_NODE_ID` | 수정완료 | F-145_injection_node_id_mismatch.md |
| F-146 | 치명 | 코드버그 | `siap/codec.py` · `siap/link.py` · 시연 §3.1 | S4-b 순서 X01→X03→X05에서 재동기 상태가 연속 위반 헤더를 버려 5종 중 2종 판정 유실 | 수정완료 | F-146_sequential_injection_resync_loss.md |
| F-147 | 오류 | 코드버그 | `tools/mode_verify.py` | 실제 `inject()` 송신 경로를 호출하지 않아 바이트 변조 구현도 8/8 거짓 통과 | 수정완료 | F-147_mode_verify_injection_blindspot.md |
| F-148 | 치명 | 요건위반 | `logs/session_00_golden.jsonl` · `sim/virtual_node.py` | 기본 replay·simulate가 골든의 고정 센서값 25.3·61.0을 로그·데모 데이터로 사용 | 기각 | F-148_hardcoded_sensor_demo_data.md |
| F-149 | 오류 | 코드버그 | `tools/where.py` · `fix_log/meta_verify.py` | F-144가 수정완료지만 회귀 테스트가 없어 필수 메타 검증 94/95 실패 | 수정완료 | F-149_f144_regression_test_missing.md |
| F-150 | 오류 | 코드버그 | `tools/mode_verify.py` | F-147 수정 후에도 실제 송신 바이트 변조를 9/9로 거짓 통과 | 수정완료 | F-150_mode_verify_wire_bytes_still_blind.md |
| F-151 | 오류 | 코드버그 | `siap/codec.py` | F-146의 Version-only 재동기가 payload의 0x12와 다음 정상 헤더를 삼킴 | 수정완료 | F-151_version_only_resync_drops_frame.md |
| F-152 | 치명 | 요건위반 | F-148 · `logs/session_00_golden.jsonl` · GPT 검증 프롬프트 | 하드코딩 센서 로그 실격 기준이 남았는데 예외 문서만 추가하고 기각 | 수정완료 | F-152_hardcoded_log_rejection_conflict.md |
| F-153 | 위험 | 문서불일치 | `DB_스키마_설계서.md` §8 vs `schema.sql` | 설계서 §8 예시 DDL이 F-083·F-085·F-091·F-092 반영 전 스냅샷이라 정본과 어긋남 | 수정완료 | F-153_db_design_doc_ddl_snapshot_stale.md |
| F-154 | 오류 | 코드버그 | `run.py` · `siap/link.py` · `backend/ingest.py` | 실제 실행 경로에 NEC alert 저장과 ACK 회신을 함께 수행할 수 있는 콜백이 없음 | 수정완료 | F-154_backend_callback_contract_disconnected.md |
| F-155 | 오류 | 코드버그 | `tools/db_live_verify.py` | `sqlite3` 별칭 연결은 탐지하지 못해 `foreign_keys=ON` 없는 우회 연결도 11/11 통과 | 수정완료 | F-155_db_verifier_alias_connect_bypass.md |
| F-156 | 오류 | 코드버그 | `backend/ingest.py` · `repository.py` | 같은 프레임의 장치상태·환경상태를 저장하고도 `operating_env` 관계를 만들지 않음 | 수정완료 | F-156_operating_environment_relation_omitted.md |
| F-157 | 오류 | 코드버그 | `backend/repository.py` | 냉난방기 단일 Value를 전원과 온도 두 필드에 중복 저장 | 수정완료 | F-157_cooling_heater_value_duplicated.md |
| F-158 | 오류 | 문서불일치 | DB 설계서 · `schema.sql` · `models.py` | 1369-P1 6.2.5 필수 설치일자를 장치설치정보가 저장하지 않음 | 수정완료 | F-158_install_date_standard_omitted.md |
| F-159 | 오류 | 문서불일치 | DB 설계서 · `schema.sql` · `repository.py` | 전역 식별자라고 한 `model_name`의 유일성을 DDL이 강제하지 않음 | 수정완료 | F-159_model_name_uniqueness_unenforced.md |
| F-160 | 오류 | 코드버그 | `run.py` · `backend/ingest.py` | F-154 수정 후에도 실제 진입점이 ingest 콜백을 주입하지 않아 정상 NEC의 alert 저장 경로가 없음 | 수정완료 | F-160_run_entrypoint_ingest_unbound.md |
| F-161 | 오류 | 코드버그 | `tools/db_live_verify.py` | F-155 수정이 대입으로 전파된 `sqlite3.connect` 별칭을 놓쳐 FK-OFF 연결도 11/11 통과 | 수정완료 | F-161_db_verifier_assignment_alias_bypass.md |
| F-162 | 오류 | 코드버그 | `schema.sql` · `openapi.json` · 검증기 2종 | F-158 설치일자가 빈 문자열이거나 API에서 생략돼도 DB 100/100·API 71/71 통과 | 수정완료 | F-162_install_date_empty_or_omitted.md |
| F-163 | 오류 | 문서불일치 | 아키텍처 §3.1 · Frame 구조 명세서 · `siap_iface.py` | F-154가 회신 책임을 link로 옮겼지만 설계·계약은 ingest 반환 회신으로 남음 | 수정완료 | F-163_ingest_reply_contract_stale.md |
| F-164 | 오류 | 문서불일치 | `CLAUDE.md` §3.5 · DB/아키텍처 설계서 | F-156의 다중 장치상태 작동환경 미결속 결정을 코드에만 두고 정본 결정 표에 기록하지 않음 | 수정완료 | F-164_operating_env_ambiguity_undocumented.md |
| F-165 | 오류 | 코드버그 | `tools/db_live_verify.py` | 타입 주석 대입으로 전파된 `sqlite3.connect` 별칭을 놓쳐 FK-OFF 연결도 11/11 통과 | 수정완료 | F-165_db_verifier_annotated_alias_bypass.md |
| F-166 | 오류 | 코드버그 | `schema.sql` · `openapi.json` · `api_verify.py` | 설치일자에 `not-a-date`를 넣어도 DB와 API 검증을 통과해 ISO 8601 계약을 강제하지 못함 | 수정완료 | F-166_install_date_format_unchecked.md |
| F-167 | 오류 | 문서불일치 | `run.py` · `siap/link.py` · 아키텍처/Frame 설계서 | F-160 구현은 지연 연결을 쓰지만 문서는 교차 스레드 오류가 나는 `ingest.bind(conn)` 결선을 권함 | 수정완료 | F-167_ingest_bind_threading_docs_stale.md |
| F-168 | 오류 | 코드버그 | `tools/db_live_verify.py` · `backend/db.py` | 팩토리 파일 자체를 검사에서 제외해 FK-OFF 신규 연결 함수를 추가해도 11/11 통과 | 수정완료 | F-168_db_factory_path_unchecked.md |
| F-169 | 오류 | 코드버그 | `backend/repository.py` · `backend/ingest.py` | 재연결 시 새 `device_info_id`를 버려 등록 정체성과 이후 측정 subtype이 서로 어긋남 | 수정완료 | F-169_device_identity_drift.md |
| F-170 | 오류 | 코드버그 | `backend/repository.py` · `backend/ingest.py` | 측정 위치를 저장하지 않고 재연결은 설치 위치·단위를 NULL로 덮어씀 | 수정완료 | F-170_measurement_metadata_lost.md |
| F-171 | 오류 | 코드버그 | `backend/ingest.py` · `backend/repository.py` | 유효범위 0~50 센서의 값 999도 오류 표시 없이 정상 측정으로 저장 | 수정완료 | F-171_out_of_range_measurement_stored.md |
| F-172 | 오류 | 코드버그 | `tools/db_live_verify.py` | `db.py` 안의 `sqlite3.connect` 별칭 호출 함수를 놓쳐 FK-OFF 연결도 15/15 통과 | 수정완료 | F-172_db_factory_alias_call_bypass.md |
| F-173 | 오류 | 코드버그 | `backend/ingest.py` | 등록 장치와 다른 `dev_type`·subtype의 값 알림을 대조하지 않고 저장해 F-169 정체성 불일치가 남음 | 수정완료 | F-173_device_value_identity_unchecked.md |
| F-174 | 오류 | 코드버그 | `tools/db_live_verify.py` | lambda로 만든 FK-OFF 연결 팩토리를 탐지하지 못해 전체 검증이 15/15 통과 | 수정완료 | F-174_db_factory_lambda_bypass.md |
| F-175 | 오류 | 코드버그 | `siap/codec.py` · `backend/ingest.py` | 등록 subtype과 같아도 `dev_type`만 뒤집으면 정상 디코드 뒤 ingest가 예외로 중단 | 수정완료 | F-175_device_type_subtype_mismatch.md |
| F-176 | 오류 | 문서불일치 | 아키텍처 §4.4-a · `backend/ingest.py` | 동적 장치 설치 뒤 `device_manage`를 만들 경로가 없어 표준의 N:1 장치관리 관계가 항상 누락 | 수정완료 | F-176_dynamic_device_manager_missing.md |
| F-177 | 오류 | 코드버그 | `backend/ingest.py` | F-171 수정이 양쪽 경계가 모두 있을 때만 검사해 편측 유효범위 밖 측정값을 저장 | 수정완료 | F-177_one_sided_range_bypass.md |
| F-178 | 오류 | 코드버그 | `backend/ingest.py` | 프레임 처리 중 예외 시 rollback하지 않아 앞 요소의 부분 저장이 후속 commit으로 확정 | 수정완료 | F-178_frame_transaction_partial_commit.md |
| F-179 | 오류 | 코드버그 | `tools/db_live_verify.py` | `getattr(sqlite3, connect)` 직접 연결을 놓쳐 FK-OFF 코드도 15/15 거짓 통과 | 수정완료 | F-179_db_verifier_getattr_bypass.md |
| F-180 | 오류 | 코드버그 | `backend/ingest.py` | F-175 수정이 값 알림만 막고 연결 등록의 Type/Subtype 불일치를 수용해 장치 정체성이 분기 | 수정완료 | F-180_connection_type_subtype_mismatch.md |
| F-181 | 오류 | 코드버그 | `tools/db_live_verify.py` | `__import__(sqlite3).connect()` FK-OFF 팩토리를 놓쳐 전체 15/15 거짓 통과 | 수정완료 | F-181_db_live_dynamic_import_bypass.md |
| F-182 | 오류 | 문서불일치 | `backend/repository.py` · 아키텍처 §4.4-a | 동적 설정형 데이터 CREATE·UPDATE가 `config_change_log`에 기록되지 않음 | 수정완료 | F-182_config_change_history_unwritten.md |
| F-183 | 오류 | 문서불일치 | `backend/ingest.py` · `repository.py` · `schema.sql` | 정상 장치 등록이 필수 설치위치·단위를 항상 NULL로 저장 | 수정완료 | F-183_install_location_missing.md |
| F-184 | 오류 | 코드버그 | `backend/schema.sql` · `db/verify.py` | F-166이 설치일자만 막아 다른 시간 컬럼은 비 ISO 문자열을 허용 | 수정완료 | F-184_time_format_constraints_incomplete.md |
| F-185 | 오류 | 문서불일치 | DB 설계서 · `schema.sql` · `models.py` | 1369-P1 6.2.4 필수 장치특성 저장 필드가 없음 | 수정완료 | F-185_device_characteristics_omitted.md |
| F-186 | 위험 | 문서불일치 | 아키텍처 §4.4-a④ · `siap_iface.py` | `control_execution` 응답 필드 UPDATE 주체가 `SiapLink.send()`의 동기 반환 계약과 어긋남 | 수정완료 | F-186_exec_result_update_thread_mismatch.md |
| F-187 | 위험 | 문서불일치 | `backend/api.py::_header_field_slices` | `Frame.fields`가 헤더 7필드만 채우고 가변부(DEVICE_MAIN_INFO 등) 필드 분해가 없음 | 수정완료 | F-187_frame_fields_payload_incomplete.md |
| F-188 | 위험 | 문서불일치 | `project_code/run.py` | `run.py`가 아직 uvicorn 을 기동하지 않아 API 서버가 실행 경로에서 뜨지 않음 | 수정완료 | F-188_run_py_uvicorn_not_wired.md |
| F-189 | 치명 | 문서불일치 | `backend/services/mms.py` · 시연 시나리오 S5 · `docs/ai-usage.md` | `llm_draft`가 항상 임계값 폴백이라 "AI가 만든 규칙 초안" 시연과 생성형 AI 활용 증거가 성립하지 않음 | 보류 | F-189_llm_draft_always_fallback.md |
| F-190 | 치명 | 코드버그 | `backend/services/mms.py` · `tools/nodetype_verify.py` | 서비스 계층에 관수 장치·33도 규칙이 하드코딩됐지만 노드종류 검증기는 보드명만 보아 통과 | 수정완료 | F-190_nodetype_verifier_misses_device_logic.md |
| F-191 | 오류 | 문서불일치 | 0937 대조표 6.4-3·6.5-2 · `backend/services/fms.py` · `backend/ingest.py` | 미수집·단절·재전송 소진 알림 진입점이 닫히지 않았는데 두 필수 요구를 ✅로 판정 | 수정완료 | F-191_service_alert_requirements_false_complete.md |
| F-192 | 오류 | 코드버그 | `tools/gate_e2e.py` | action=NULL 승인 반례가 없어 이를 허용하는 잘못된 API도 16/16 통과 | 수정완료 | F-192_gate_verifier_misses_null_action.md |
| F-193 | 오류 | 코드버그 | `fix_log/meta_verify.py` · `fix_log/F-187*` · `fix_log/F-188*` | 인덱스에 없는 상세 파일 2개를 놓치고 102/102 통과 | 수정완료 | F-193_meta_verifier_misses_orphan_details.md |
| F-194 | 오류 | 코드버그 | `tools/nodetype_verify.py` | 장치명 토큰 6종 밖의 디바이스 하드코딩을 놓쳐 CLAUDE.md §1-6 위반 코드가 통과 | 수정완료 | F-194_nodetype_unlisted_device_bypass.md |
| F-195 | 오류 | 코드버그 | `tools/services_verify.py` | 실행 불가능한 dead code 호출 흔적만 있어도 0937 알림 결선이 구현된 것으로 통과 | 수정완료 | F-195_services_verify_dead_code_bypass.md |
| F-196 | 오류 | 코드버그 | `project_docs/web/web_verify.py` | 실제 구현 경로 `project_code/web/`을 찾지 못해 구현 검사를 전부 건너뜀 | 수정완료 | F-196_web_verify_wrong_impl_path.md |
| F-197 | 오류 | 코드버그 | `tools/web_live_verify.py` | `api.js` 밖의 직접 fetch를 검사하지 않아 존재하지 않는 외부·내부 호출도 통과 | 수정완료 | F-197_web_live_direct_fetch_blindspot.md |
| F-198 | 치명 | 코드버그 | `project_code/run.py` · `project_code/web/` | 기본 simulate 서버에 디바이스가 없어 웹 핵심 기능 3종을 재현할 수 없음 | 수정완료 | F-198_simulate_web_has_no_devices.md |
| F-199 | 오류 | 코드버그 | `project_code/web/rules.html` | 규칙 초안을 이스케이프 없이 `innerHTML`에 넣어 저장형 DOM 주입이 가능함 | 수정완료 | F-199_rule_draft_dom_injection.md |
| F-200 | 위험 | 문서불일치 | `project_code/web/static/stream.js` · `verify.html` | SSE 재연결 뒤 설계된 `since` 기반 누락 프레임 복구가 없음 | 수정완료 | F-200_sse_reconnect_gap_not_recovered.md |
| F-201 | 오류 | 문서불일치 | 화면 설계서 §2.1 · `project_code/web/` | 화면↔API 대응표가 실제 읽기 호출 4종을 누락했는데 검증기 62/62·16/16 통과 | 수정완료 | F-201_web_api_mapping_incomplete.md |
| F-202 | 오류 | 문서불일치 | `project_docs/api/openapi.json` · `schema.sql` | F-091 수정완료 뒤에도 거부 제약 수치가 CHECK 3·트리거 3으로 남아 실제 4·1과 불일치 | 수정완료 | F-202_reject_constraint_counts_stale.md |
| F-203 | 오류 | 코드버그 | `contracts/frame.py` · `backend/ingest.py` · `web/` | 수신 Frame.t가 항상 0이라 모든 프레임·노드 시각이 1970년으로 표시됨 | 수정완료 | F-203_frame_receive_time_zero.md |
| F-204 | 오류 | 코드버그 | `tools/web_live_verify.py` · `web/rules.html` | 헬퍼가 미승인 실행 폼을 렌더링하면 실제 실행 버튼이 있어도 웹 검증기 23/23·68/68 통과 | 신규 | F-204_pending_execute_helper_bypass.md |
| F-205 | 위험 | 코드버그 | `web/verify.html` · `tools/web_live_verify.py` · F-200 | 재연결 누락 복구가 단일 `limit=100` 요청이라 151건 중 51건 영구 누락 | 신규 | F-205_sse_recovery_still_limited.md |
| F-206 | 오류 | 코드버그 | `tools/layer_verify.py` | `project_code` 전체 금지를 선언하지만 `project_docs` import는 `contracts/`만 검사해 backend 위반도 7/7 통과 | 신규 | F-206_layer_verify_project_docs_scope.md |
| F-207 | 오류 | 문서불일치 | 골든벡터 명세서 §3.1·§6 | 실제 53+5건·판정 9/1/43인데 57건·7/1/44로 남아 F-124 수정완료 주장과 불일치 | 신규 | F-207_golden_spec_verdict_counts_stale.md |
| F-208 | 오류 | 문서불일치 | `contracts/siap_iface.py` · `siap/build.py` · `contracts/fake_link.py` | 연결 실패 응답이 RSC만 싣는다는 계약 설명이 표준 및 실제 9byte 고정부 구현과 반대 | 신규 | F-208_connection_error_payload_contract.md |
| F-209 | 오류 | 문서불일치 | Frame 구조 명세서 · `contracts/frame.py` · F-134 | 표준 미규정 결정을 `standard-findings.md`에 등재한다는 폐기된 규칙이 단계 1 정본에 잔존 | 신규 | F-209_undefined_decision_target_regression.md |
| F-210 | 오류 | 코드버그 | `firmware/core/node_state.c` · `test_node_state.c` · F-071 | 수신 `NOTI_REBOOT`를 ACK하지 않지만 기존 상태 테스트 91/91 통과 | 신규 | F-210_noti_reboot_ack_missing.md |
| F-211 | 오류 | 코드버그 | `firmware/core/` · `tools/core_purity_verify.py` | 설계상 금지된 `string.h`가 현재 core 3파일에 있고 임의 플랫폼 헤더도 순수성 검증기 7/7 통과 | 신규 | F-211_core_purity_platform_header_gap.md |
| F-212 | 오류 | 코드버그 | `tools/xcodec_verify.py` · `firmware/tests/dump_golden.c` | 양쪽 코덱이 골든 바이트를 디코드한 뒤 재인코딩해 같은 의미 오류를 가진 Python 코덱도 9/9 통과 | 신규 | F-212_xcodec_semantic_roundtrip_blindspot.md |
| F-213 | 오류 | 코드버그 | `siap/link.py` · `siap/registry.py` | 노드발 설정 요청 3종에 SUCCESS를 회신하지만 노드·프로파일 상태를 반영하지 않음 | 신규 | F-213_reverse_settings_not_applied.md |
| F-214 | 오류 | 코드버그 | `siap/codec.py` | INT·UINT 값 1.5를 거부하지 않고 1로 조용히 절삭 | 신규 | F-214_fractional_integer_truncation.md |
| F-215 | 오류 | 코드버그 | `siap/codec.py` · `siap/tests/test_codec.py` | 공개 `decode_frame()`이 불완전 입력에서 violations Frame 대신 `IncompleteFrameError`를 던짐 | 신규 | F-215_decode_incomplete_exception.md |
| F-216 | 오류 | 코드버그 | `tools/mode_verify.py` · `sim/virtual_node.py` | 골든에 없는 새 센서값을 써도 단계 4 검증기 11/11 통과 | 신규 | F-216_mode_verify_sensor_source_blindspot.md |
| F-217 | 오류 | 코드버그 | `sim/virtual_node.py` | 센서·미등록 디바이스 제어를 적용하고 SUCCESS로 회신 | 신규 | F-217_virtual_node_invalid_control_success.md |
| F-218 | 오류 | 코드버그 | `sim/replayer.py` · `run.py` | replay가 tx 기대 출력을 대조하지 않아 틀린 출력 로그도 성공 | 신규 | F-218_replay_tx_expectation_ignored.md |
| F-219 | 오류 | 코드버그 | `sim/replayer.py` · `run.py` | replay 작업 스레드가 로그 파싱 예외로 죽어도 종료 코드 0 | 신규 | F-219_replay_worker_error_success.md |
| F-220 | 오류 | 코드버그 | `backend/services/ems.py` · `backend/repository.py` | 주기만 부분 PATCH해도 기존 이벤트 임계값 두 개를 NULL로 지움 | 신규 | F-220_partial_patch_clears_thresholds.md |
| F-221 | 오류 | 코드버그 | `backend/services/ems.py` · `backend/repository.py` · 아키텍처 §4.4-a | 사용자 설정 변경이 I/O 소유 테이블을 API 스레드에서 쓰면서 변경 이력을 남기지 않음 | 신규 | F-221_property_change_history_missing.md |
| F-222 | 오류 | 코드버그 | `tools/db_live_verify.py` | `importlib.import_module(sqlite3).connect`로 foreign_keys 없는 연결을 만들어도 15/15 통과 | 신규 | F-222_db_verify_importlib_bypass.md |
| F-223 | 오류 | 문서불일치 | 아키텍처 §4.4-a · F-186 | 수정 표는 API 스레드인데 본문은 I/O 스레드 UPDATE라고 남아 있음 | 신규 | F-223_execution_owner_text_regression.md |
| F-224 | 오류 | 문서불일치 | 아키텍처 §4.4-a · `backend/repository.py` · F-198 | 디바이스 등록 트리거를 고친 표와 달리 본문·주석은 REQ_SET_CONNECTION으로 남아 있음 | 신규 | F-224_device_trigger_text_regression.md |
| F-225 | 오류 | 코드버그 | `tools/gate_e2e.py` | 거부 응답 전에 구동기 프레임을 송신하는 변이도 19/19 통과 | 신규 | F-225_gate_tx_side_effect_unchecked.md |
| F-226 | 오류 | 코드버그 | `tools/nodetype_verify.py` | `backend/api.py`의 디바이스 종류 하드코딩을 넣어도 검증기가 통과 | 신규 | F-226_nodetype_api_scope_gap.md |
| F-227 | 오류 | 코드버그 | `backend/services/fms.py` · F-191 | 수정완료 기록과 달리 미수집 기준이 디바이스 Period×3이 아닌 고정 900초 | 신규 | F-227_f191_period_threshold_unfixed.md |
| F-228 | 오류 | 코드버그 | `backend/api.py` · `backend/services/ems.py` | OpenAPI가 금지한 추가 필드와 14bit 초과 Period를 HTTP 200으로 수용 | 신규 | F-228_runtime_device_property_schema_unenforced.md |
| F-229 | 오류 | 문서불일치 | `services/0937_요구사항_대조표.md` · `services_verify.py` | 구현 후에도 구현 전 진입점 10개를 근거로 삼고 42/42 통과 | 신규 | F-229_services_matrix_implementation_drift.md |
| F-230 | 오류 | 코드버그 | `web_live_verify.py` · `web_verify.py` | CSS `@import` 외부 CDN을 넣어도 단계 7 출구가 통과 | 신규 | F-230_css_cdn_import_unchecked.md |
| F-231 | 오류 | 코드버그 | `web_live_verify.py` · `web_verify.py` | HTML 인라인 비트 언팩·숫자 코드 매핑을 넣어도 단계 7 출구가 통과 | 신규 | F-231_inline_bit_unpack_unchecked.md |
| F-232 | 오류 | 코드버그 | `web_live_verify.py` · `web_verify.py` | 프레임 목록에서 아이콘·상태 문자를 제거해 색만 남겨도 단계 7 출구가 통과 | 신규 | F-232_color_only_status_unchecked.md |

> **결번**: F-018 ~ F-021. 규약 §2에 따라 번호는 재사용하지 않는다.
> 직전 라운드에서 Claude가 수신한 인덱스에는 해당 행이 존재하지 않았다 (F-023 처리 기록 참조).

---

## 현황

| 상태 | 건수 | | 분류 | 건수 |
|---|---|---|---|---|
| 신규 | 29 | | 요건위반 | 3 |
| 확인 | 0 | | 코드버그 | 129 |
| 수정완료 | 178 | | 문서불일치 | 77 |
| 기각 | 1 | | 표준결함 | 19 |
| 보류 | 1 | | **합계** | **228** |
| 이관 | 19 | | | |

> F-052 는 요청받은 문서화를 마쳐 `수정완료`지만, 그 안에 **보류 결정 1건**(온실관리 기반 인가 제약)이
> 들어 있다. 상태가 아니라 처리 기록에 사유와 재검토 시점을 남겼다.

> **제출 패키징 직전 처리할 것 (보류 1건)**
> **F-052** 의 인가 제약 — 일정 여유가 있을 때만.
>
> F-070(개인 경로)은 표준 원문이 저장소 안으로 들어오면서 보류 사유가 사라져 2026-08-07 에
> 앞당겨 처리했다. `meta_verify.py` 가 이제 개인 경로를 전수로 검사하므로 재발하면 즉시 실패한다.

> 이 표는 `meta_verify.py`가 실제 행 집계와 대조한다 (F-043). 3차에는 표기(`코드버그 8 /
> 문서불일치 15`)와 실제(`9 / 14`)가 달랐고, 그 불일치를 스크립트가 처음 실행에서 잡아냈다.

---

## 처리 이력

| 일시 | 라운드 | 내용 |
|---|---|---|
| 2026-08-12 | 단계 7 GPT 재검증 | F-196~F-203(오류 7·치명 1) 전량 수정, 기각 0 — 코드버그(F-196·F-203)→문서불일치(F-197·F-199·F-200·F-201·F-202)→F-198(치명, 코드버그이나 범위가 커 별도 처리) 순으로 진행하되, 요건위반 없음. **F-196(코드버그)**: `web_verify.py`의 `WEB_DIR` 후보 경로 두 곳(저장소 루트 `web/`, `project_docs/web/web/`) 모두 존재하지 않아 구현 검사 62/62 전체가 조용히 건너뛰어졌다 — 첫 후보에 `project_code/web/`를 추가, 68/68로 실물 검사가 실제로 켜짐을 확인. **F-197(문서불일치)**: `tools/web_live_verify.py`가 `api.js`의 `request()` 래퍼 정의만 보고 화면·모듈이 그 래퍼를 우회해 직접 `fetch()`를 부르는 경로(존재하지 않는 라우트·외부 URL 포함)를 놓쳤다 — api.js 밖 `fetch()` 전량 위반 검사·api.js 자신의 fetch() 1회 제한·외부 URL 리터럴 직접 호출 금지 3종 신설. **F-199(문서불일치)**: `rules.html`이 `rule.draft_text`·`condition_expr`·`reject_reason`(서버 저장 자유 텍스트)을 `escapeHtml()` 없이 템플릿 리터럴에 그대로 보간해 저장형 DOM 주입이 가능했다 — `fmt.js`에 `escapeHtml()` 신설, 5개 렌더 함수 전부에 적용, 회귀 검사 신설. **F-198(치명, 코드버그) — 가장 무겁다**: `backend/ingest.py::_handle_connection()`이 `REQ_SET_CONNECTION`(0943 8.1.1, 페이로드 없음 LAYOUT (0,0))에 바인딩돼 있어 `device_properties`가 구조적으로 항상 비어 simulate/웹 대시보드에 장치가 하나도 뜨지 않았다(원 신고) — 독립 GPT 검증이 초기 수정안(디스패치 교정만)을 "근본 원인은 맞지만 registry 반영·재전송·문서·테스트 경로가 전부 빠졌다"고 반려, 사용자가 GPT 권고 전체 범위 즉시 구현을 선택. `backend/ingest.py`(dispatch를 `REQ_SET_DEVICE_PROPERTY`/`REQ_SET_NODE_DEVICE_PROPERTY_ALL`로 교정)·`siap/registry.py`(`merge_device_properties` 신설)·`siap/link.py`(레지스트리 결선)·`sim/_wire.py`(독립 인코더로 `WireNP`/`WireDP`/`build_req_set_node_device_property_all` 신설)·`sim/virtual_node.py`(RES_SET_CONNECTION 성공 직후 1회 선언 + 최대 3회 재전송)까지 5개 파일 수정, 기존 backend 테스트 6곳의 "코덱이 만들 수 없는 Frame" 픽스처도 교정. 새 결선 검증 2종 신설(`siap/tests/test_link.py`의 wire→codec→link→registry 전체 경로, `tools/mode_verify.py`의 신선한 DB 실측 확인) — 후자를 만들며 Windows `TemporaryDirectory` 정리 시 SQLite 잠금 충돌을 `ignore_cleanup_errors=True`+best-effort `close()`로, 테스트 동기화를 `threading.Event` 2단계로 각각 해소. 문서 수정 중 아키텍처 설계서 §3.1의 F-079(0937 6.1 EMS) 미구현 업무 로직 주석을 실수로 지워 `services_verify.py`가 41/42로 즉시 회귀 — 원인을 `fix_log/F-079_*.md`로 추적해 등록(F-198, 구현됨)과 업무 로직(F-079, 미구현) 주석을 분리 병기하는 형태로 복원, 42/42 재확인. `CLAUDE.md` §3.5에 "노드가 디바이스 구성을 게이트웨이에 선언하는 시점" 결정 신설. 실물 펌웨어(단계 8)는 같은 절차가 아직 없음을 아키텍처 설계서 §5.5에 후속 과제로 명시(이번 범위 아님). **결함 주입 + 실측 검증**: `run.py --mode simulate --serve`를 신선한 DB로 실행해 `/devices`·`/telemetry`·`/device-states`가 실제로 채워짐을 curl로 직접 확인(원 재현의 역재현). **F-203(코드버그)**: `codec.decode_frame()`은 순수 함수라 `Frame.t`를 채우지 않는데(골든 벡터 재생·단독 코덱 테스트의 결정론 유지를 위해 의도적) 이를 채우는 코드가 어디에도 없어 `frame_log.t`가 항상 0(1970-01-01)으로 저장됐다 — `siap/link.py::_io_loop()`에서 디코더가 프레임을 내보내는 순간 `dataclasses.replace(frame, t=time.time())`로 실수신 시각을 채움(`Frame`이 `frozen=True`라 교체 방식). 결함 주입(그 한 줄 제거) 후 신규 테스트가 `assert 0.0 != 0.0`으로 즉시 실패함을 확인. 실측: `run.py --mode simulate --serve`를 신선한 DB로 6초 구동 → `frame_log`가 `(22, 22, ...)`(전건 서로 다른 실측 epoch, count=count(distinct t)), `/api/v1/nodes`의 `connected_at`·`last_seen_at`도 2026-08-12 실제 시각으로 표시됨을 확인(원 신고의 1970-01-01 재현과 반대 결과). **F-201(문서불일치)**: `화면_설계서.md` §2.1 대응표가 실제 읽기 호출 4종(`verify.html`의 `listAlerts`, `rules.html`의 `listPublicDataSources`·`listNodes`·`listNodeDevices`)을 누락했는데, `web_verify.py`(표↔openapi.json)와 `web_live_verify.py`(구현↔api.js)가 서로 반대쪽만 봐서 62/62·16/16으로 통과했다 — 표에 4건 추가하고, `web_live_verify.py`에 §2.1 표를 직접 파싱해 화면별 GET 호출과 양방향 대조하는 검사 신설(20→22항목). **F-202(문서불일치)**: F-091이 화면 설계서·DDL은 "CHECK 4종·트리거 1종"으로 맞췄지만 `openapi.json`의 `rejectRule` 설명만 옛 수치("CHECK 3종과 트리거 3종")로 남았다 — 문구 교체 후, `meta_verify.py`의 기존 F-091 회귀 검사(Markdown만 대조)가 놓친 JSON 설명 필드를 별도로 대조하는 검사 신설(109→110항목). **F-200(위험, 문서불일치)**: 화면_설계서.md §3.2(신고문은 §10으로 오기, 재확인 후 정정) "누락 보정" 행이 재연결 직후 `listFrames?since=`로 폴백 중 놓친 프레임을 채우도록 정하는데 `stream.js`·`verify.html` 어디에도 그 결선이 없었다(백엔드는 `since` 파라미터·`_iso_to_epoch` 변환까지 이미 갖춰져 있어 프런트만 비어 있었다) — `stream.js`에 재연결(최초 연결 제외)을 알리는 `onReconnect` 콜백 신설, `fmt.js`에 `epochToIso()` 신설, `verify.html`에 `recoverMissedFrames()`(`state.frames[0].t`를 커서로 `since=` 조회 후 오래된 것부터 upsert)를 신설해 결선. `web_live_verify.py`에 이 결선 존재를 텍스트로 확인하는 검사 신설(22→23항목) — 브라우저 없이 실제 단절·재연결을 재현할 수 없다는 원 신고의 한계는 그대로 남긴다. **회귀테스트**: `siap/tests/test_link.py` +2(F-198 1·F-203 1), `tools/web_live_verify.py` +4(F-197 3·F-199 1·F-200 1·F-201 1 — 표기상 F-197 은 기존 세 검사 신설을 이 라운드에서 집계), `tools/mode_verify.py` +1(F-198), `fix_log/meta_verify.py` +1(F-202). 검증: `cd project_code && python -m pytest siap/tests/ backend/tests/ sim/tests/` **375/375** · `python -m pytest tools/tests/` **28/28** · `python tools/web_live_verify.py` **23/23** · `python project_docs/web/web_verify.py` **68/68** · `python fix_log/meta_verify.py` **110/110** · `python project_docs/api/api_verify.py` **71/71** · `python project_docs/services/services_verify.py` **42/42**(F-079 회귀 포함) · `python project_docs/dev/dev_verify.py` **76/76** · `python tools/run_all.py` 재확인 |
| 2026-08-11 | 단계 5 GPT 8차 재검증 | F-181~F-185(오류 5) 전량 수정, 기각 0 — 코드버그(F-181·F-184)→문서불일치(F-182·F-183·F-185) 순, 같은 분류 안에서는 인덱스 순서. **F-181(코드버그)**: `module_aliases`가 `ast.Import`로 만든 이름 바인딩만 모아 `__import__('sqlite3').connect(...)`(모듈을 이름에 전혀 묶지 않는 표현식)를 놓쳤다 — `_is_dunder_import_sqlite()` 신설, `.connect` 속성 판정·`getattr` 첫 인자 판정·(신설) 모듈 별칭 판정(`x = __import__('sqlite3')`를 `import sqlite3 as x`와 같은 자격으로 편입) 세 자리 모두에 반영. **F-184(코드버그)**: F-166이 형식(ISO 8601 GLOB) 검사를 `installed_at` 하나에만 걸어 나머지 시간 컬럼은 비 ISO 문자열을 그대로 저장했다 — `schema.sql`의 모든 시간 TEXT 컬럼(약 20개)에 같은 최소 GLOB 검사를 추가(nullable 컬럼은 `IS NULL OR GLOB` 패턴). 부수 정리: 형식 검사가 넓어지자 `verify.py`·`test_schema_conformance.py`가 시간 컬럼 자리에 광범위하게 써 온 `'t'`/`'t2'` 비-ISO 자리표시자 때문에 기존 6개 케이스가 FAIL로 돌아서, 두 파일의 해당 리터럴 전부(파일당 35개소)를 유효 ISO 값으로 스크립트 치환했다(대체 전 정확히 6 FAIL 확인 후 대체, 0 FAIL 재확인). **F-182(문서불일치) — 판정: 표준이 옳고 설계·구현이 틀렸다**: 아키텍처 설계서 §4.4-a가 `config_change_log`를 "시드 로더 전용"으로 잘못 분류해(F-176의 `device_manage`와 같은 오분류 패턴) 런타임 설정형 데이터 변경(1369-P1 6.2.1) 이력이 항상 0건이었다 — `repository.py`에 `record_config_change()` 신설, 실제로 CREATE/UPDATE를 만드는 `get_or_create_device_info()`·`upsert_device_install_info()`가 그 시점에 직접 호출하도록 결선(재사용은 이력 없음). §4.4-a에서 ①(8→**7**)에서 ②(20→**21**)로 이동. **F-183(문서불일치) — 판정: 표준이 옳고 설계·구현이 틀렸다**: 0943 REQ_SET_CONNECTION은 설치위치를 나르지 않고 이 참조 구현엔 별도 입력 수단이 없어(F-176과 같은 사정) `install_location`/`install_loc_unit`이 항상 NULL이었다 — 지어내지 않고(CLAUDE.md §1-1) 그 장치가 설치된 온실 자신의 위치를 기본값으로 쓴다(`repository.get_greenhouse_location`). **최초 등록에서만** 이 기본값을 넘기도록 했다 — 재연결마다 넘기면 F-170의 COALESCE 보존 의미론이 매번 온실 기본값으로 되돌아가는 형태로 재발하기 때문에, `_handle_connection()`이 `find_device_install_by_siap()`로 신규/재연결을 먼저 가른다. **F-185(문서불일치) — 판정: 표준이 옳고 설계·구현이 틀렸다**: 6.2.4 "장치정보에는... 장치특성 등이 포함되어야 한다"인데 저장 컬럼 자체가 없었다 — `manufacturer`와 같은 자격(nullable, 0943 미규정 속성)으로 `device_characteristics` 컬럼 추가(schema.sql·models.py·repository.py), 0943 동적 등록 경로는 채우지 않는다. **파급 정리**: DB 제약 테스트 103→**109**종(F-184 +5, F-185 +1) — `DB_스키마_설계서.md`(§0·§6.2·§8 전체 DDL 임베드 재동기)·`개발_착수_지시서.md`·`0937_요구사항_대조표.md`의 "103종" 인용 갱신, 아키텍처 설계서 분량 증가(33,000→**37,000자**)로 `개발_착수_지시서.md` 경로표 갱신(F-182 각주 추가로 인한 자연 증가, `dev_verify.py`의 10% 오차 검사가 실측으로 잡아냈다). `project_docs/db/schema.sql`이 하드링크가 아니라 별도 파일로 갈라져 있었다(Edit 도구의 원자적 쓰기가 하드링크를 끊음, F-153 파일 동기 전제가 이번 세션부터 깨져 있었다) — `cp`로 즉시 재동기, 이후 매 schema.sql 수정마다 재확인. **회귀테스트**: `tools/tests/test_db_live_verify.py` +4(17→**21**), `backend/tests/test_ingest.py` +6(F-182 2·F-183 2·F-184는 스키마 레벨이라 backend 회귀 불필요), `backend/tests/test_repository.py` +7(F-182 3·F-183 2·F-185 2), `project_docs/db/verify.py`+`backend/tests/test_schema_conformance.py` 각 +6(F-184 5·F-185 1). 검증: `cd project_code && python -m pytest backend/tests/` **185/185** · `python -m pytest siap/tests/ backend/tests/` **287/287** · `python -m pytest tools/tests/` **28/28** · `python tools/db_live_verify.py` **15/15** · `python project_docs/db/verify.py` **109/109** · `python project_docs/dev/dev_verify.py` **76/76** · `python project_docs/services/services_verify.py` **42/42** · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` 재확인 |
| 2026-08-11 | 단계 5 GPT 7차 재검증 | F-176~F-180(오류 5) 전량 수정, 기각 0 — 코드버그(F-177~F-180)→문서불일치(F-176) 순, 코드버그 안에서는 인덱스 순서. **F-177(코드버그)**: F-171의 범위 검사가 `lo is not None and hi is not None`로 두 경계가 모두 있을 때만 발동해, 스키마가 독립적으로 nullable 허용하는 편측 유효범위(하한만/상한만)를 우회시켰다 — `(lo is not None and value < lo) or (hi is not None and value > hi)`로 각 경계를 독립 검사하도록 수정. **F-178(코드버그)**: `handle()`이 성공 경로 끝에서만 `commit()`을 걸고 예외 경로에 rollback이 없어, SQLite `RAISE(ABORT,...)`가 현재 문장만 되돌리고 트랜잭션은 열어 두는 특성과 맞물려 실패 프레임의 앞선 요소 INSERT가 열린 트랜잭션에 남았다 — 전체를 `try/except`로 감싸 예외 시 `rollback()` 후 재전파. **F-179(코드버그)**: 공유 별칭 헬퍼가 `.connect` 속성 접근과 이름 별칭만 인식하고 `getattr(sqlite3, "connect")`(별개의 `ast.Call` 표현식)는 두 판정 모두에서 놓쳤다 — `_is_getattr_connect()` 신설, 대입 우변 판정과 직접 호출 판정 두 자리 모두에 연결. **F-180(코드버그)**: F-175가 `_handle_device_value()`에만 Type/Subtype 일관성 가드를 넣어 `_handle_connection()`(최초 등록)에는 같은 불변식이 없었다 — 같은 가드를 등록 경로에도 추가(F-176과 같은 함수라 한 커밋으로 반영). **F-176(문서불일치) — 판정: 표준이 옳고 설계·구현이 틀렸다**: `schema.sql`의 `device_manage` 테이블 주석 자체가 이미 1369-P1 §7.1(7)을 근거로 `UNIQUE(install_id)`를 걸어 뒀는데(스키마 설계 의도는 정확했다), 아키텍처 설계서 §4.4-a가 이 테이블을 "시드 로더 전용·런타임 쓰기 금지"로 잘못 분류했고 `_handle_connection()`이 그 분류를 따라 이 관계를 결코 채우지 않았다 — `repository.py`에 `get_greenhouse_manager_user_id()`·`link_device_manage()` 신설, `_handle_connection()`이 `link_device_install()` 직후 호출하도록 결선(관리자는 그 장치가 설치된 온실의 관리자를 그대로 씀, §3.5 신규 결정). 파급: 아키텍처 설계서 §4.4-a에서 `device_manage`를 ①(9→**8**)에서 ②(19→**20**)로 이동, `9+19+2+1`→`8+20+2+1=31` 갱신, `fixtures/seed.sql` 머리말 주석 정정. **결함 주입 검증**: F-179는 재현과 동일한 두 형태(대입 별칭·직접 호출)로 새 헬퍼가 실제로 잡는지 확인. **회귀테스트**: `backend/tests/test_ingest.py` +6(F-176 2·F-177 2·F-178 1·F-180 1), `backend/tests/test_repository.py` +3(F-176), `tools/tests/test_db_live_verify.py` +3(F-179). 검증: `cd project_code && python -m pytest backend/tests/` **168/168** · `python -m pytest siap/tests/ backend/tests/` **270/270** · `python -m pytest tools/tests/` **24/24** · `python tools/db_live_verify.py` **15/15** · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py`의 "쓰기 소유권 분류 파싱"이 문서를 다시 파싱해 `값=8/20/2/1`·"테이블 31개 전부 덮음"을 자동 재확인(하드코딩 없음) — **97/97** |
| 2026-08-11 | 단계 5 GPT 6차 재검증 | F-174~F-175(오류 2) 전량 수정, 기각 0 — 인덱스 순서대로, 둘 다 직전 라운드(F-172·F-173) 수정의 회귀. **F-174**: F-172가 별칭 해석을 공유 헬퍼로 통합했지만 "무엇을 팩토리 후보로 볼 것인가"는 여전히 `ast.FunctionDef` 하나로 고정돼 있어, `unsafe_connect = lambda db_path: sqlite3.connect(str(db_path))`처럼 모듈 최상위 lambda 대입 팩토리가 후보에서 아예 빠졌다 — `_db_factory_functions()`에서 `ast.Assign`/`ast.AnnAssign`의 값이 `ast.Lambda`인 경우도 같은 자격으로 검사하도록 확장. `_function_sets_fk_pragma()`는 고치지 않았다 — 람다는 표현식 하나뿐이라 PRAGMA를 자기 본문에 걸 수 없으므로 기존 "이름 못 찾으면 False" 동작 자체가 이미 올바른 판정이었다. **F-175**: F-173은 등록 subtype과 알림 subtype만 비교했는데, 표 7-14의 `dev_type`은 subtype과 **독립된 1bit 필드**라 codec.py가 조합 자체를 거부하지 않는다(같은 HUMIDITY subtype에 ACTUATOR dev_type을 실어도 정상 디코드) — subtype이 일치해도 `record_env_measurement`/`record_device_state` 분기가 엉뚱한 서브타입 집합을 골라 `ValueError`로 죽었다. **수정 위치를 `siap/codec.py`가 아니라 `backend/ingest.py`로 한정**했다 — `RSC.INVALID_DEVICE_TYPE`(표 7-10)을 코덱이 실제로 판정하게 만드는 것은 위반 8종 표에 9번째 케이스를 추가하는 `contracts/` 변경(골든 벡터 신설 + C/Python 양쪽 반영, CLAUDE.md §5 절차 대상)이라 이번 신고 범위를 넘어선다고 판단해 처리 기록에 사용자 보고로 남기고, 대신 `ingest.py`에 `dmi.dev_type is not Subtype(dmi.subtype).dev_type`이면 건너뛰는 backend 계층 무결성 가드만 추가했다(DB join 없이 계약에 이미 있는 `Subtype.dev_type` 프로퍼티만 사용). **결함 주입 검증**: F-174는 재현과 동일한 lambda PoC로 새 로직이 실제로 잡는지 확인. **회귀테스트**: `tools/tests/test_db_live_verify.py` +1(13→**14**), `backend/tests/test_ingest.py` +1(F-175). 검증: `cd project_code && python -m pytest backend/tests/` **159/159** · `python -m pytest siap/tests/ backend/tests/` **261/261** · `python -m pytest tools/tests/` **21/21** · `python tools/db_live_verify.py` **15/15**(항목 수 불변) · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` 재확인 |
| 2026-08-11 | 단계 5 GPT 5차 재검증 | F-172~F-173(오류 2) 전량 수정, 기각 0 — 인덱스 순서대로 처리, 둘 다 직전 라운드(F-168·F-169) 수정의 회귀. **F-172**: F-168이 신설한 `_db_factory_functions`가 리터럴 `sqlite3.connect(` 판정만 새로 만들어 썼는데, 정작 같은 파일에 이미 있던 `_find_sqlite_connect_bypasses`(F-155/F-161/F-165)의 대입 별칭 해석 로직을 재사용하지 않아 `_open = sqlite3.connect; _open(...)` 같은 별칭 호출을 놓쳤다 — 별칭 수집(`_collect_sqlite_connect_aliases`)과 호출 판정(`_is_sqlite_connect_call`)을 공유 헬퍼로 분리해 두 탐지 함수 모두 하나의 판정만 쓰도록 리팩터(판정 로직이 갈라져 한쪽만 고쳐지는 사고의 재발 방지). **F-173**: F-169가 재연결 UPDATE에 `device_info_id`를 추가해 "재연결로 종류가 바뀌는" 경로는 막았지만, `_handle_device_value()`는 여전히 (node,device) 번호만으로 설치 행을 찾고 알림의 subtype을 등록값과 대조하지 않아, 재연결 없이 다른 종류의 값 알림만 보내도 그대로 저장됐다(F-169 자신의 회귀 테스트는 등록·알림 subtype이 일치하는 경우만 다뤄 이 경로를 못 봤다) — `install["siap_subtype"] != dmi.subtype`이면 그 요소를 건너뛰도록 추가. `ENV_SUBTYPES`∩`DEVICE_STATE_SUBTYPES`=∅(실측 확인)라 subtype 코드 대조 하나로 `dev_type` 불일치까지 함께 걸린다. **결함 주입 검증**: F-172는 재현과 동일한 대입 별칭 PoC로 새 헬퍼가 실제로 잡는지 확인. **회귀테스트**: `tools/tests/test_db_live_verify.py` +1(12→**13**), `backend/tests/test_ingest.py` +1(F-173). 검증: `cd project_code && python -m pytest backend/tests/` **158/158** · `python -m pytest siap/tests/ backend/tests/` **260/260** · `python -m pytest tools/tests/` **20/20** · `python tools/db_live_verify.py` **15/15**(리팩터 후 항목 수 불변) · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` 재확인 |
| 2026-08-11 | 단계 5 GPT 4차 재검증 | F-168~F-171(오류 4) 전량 수정, 기각 0 — 전부 코드버그, 인덱스 순서대로 처리. **F-168**: `tools/db_live_verify.py`의 정적 우회 스캔이 `db.py`를 (정당하게) 제외하고, 런타임 검사는 `init_db()`·`connect()` 두 이름만 하드코딩 호출해 같은 파일에 PRAGMA 없는 새 연결 함수(`unsafe_connect`)를 추가해도 11/11 거짓 통과했다 — `db.py` 안에서 `sqlite3.connect(`를 직접 부르는 최상위 함수를 AST로 자동 탐지하고, 그 함수 **자기 본문**에 `foreign_keys=ON`이 있는지 정적으로, 실제 호출 결과의 `PRAGMA foreign_keys`를 동적으로 각각 검사하는 헬퍼 3종 신설(11→**15/15**). **F-169**: `repository.upsert_device_install_info()`의 재연결 UPDATE 절에 `device_info_id`가 빠져 있어, subtype이 바뀌는 재연결(TEMP→HUMIDITY) 후에도 예전 모델을 계속 참조했다 — SET 절에 `device_info_id=?` 추가. **F-170**: 같은 UPDATE 절이 `install_location`·`install_loc_unit`·`unit`을 호출자가 넘긴 `None`으로 그대로 덮어써 재연결마다 지웠다 — `COALESCE(?, 기존컬럼)`으로 바꿔 실제 값이 있을 때만 갱신하도록 수정(F-169와 같은 함수라 한 커밋으로 함께 처리). `ingest.py::_handle_device_value`도 `record_env_measurement()`에 `location`/`location_unit`을 넘기지 않던 누락을 고쳤다. **F-171**: 센서 값 알림이 설치 시 등록된 `lower_limit`/`upper_limit`과 대조 없이 그대로 저장됐다(1369-P1 §6.3.2 "유효범위 밖은 측정 오류로 보고 무시") — `_handle_device_value`에 범위 검사(경계값 포함)를 추가해 벗어난 값은 저장하지 않고 건너뛴다. 전용 `alert.kind`는 추가하지 않았다 — 유일한 후보인 `THRESHOLD`는 `0937_요구사항_대조표.md` §5-6이 이미 다른 의미(0943 표 7-15 업무 임계값 이벤트, 미구현)로 예약해 둔 값이라 재사용하면 두 개념이 섞이고, 새 `kind` 추가는 `schema.sql` 2곳+설계서+`verify.py` 건수 갱신이 딸린 스키마 변경이라 이번 코드버그 수정 범위를 넘어선다고 판단해 처리 기록에만 남겼다(사용자 보고 대상). **결함 주입 검증**: F-168은 재현과 동일한 `unsafe_connect()`를 담은 임시 `db.py`로 새 헬퍼가 실제로 잡는지 확인. **회귀테스트**: `tools/tests/test_db_live_verify.py` +2(10→**12**), `backend/tests/test_repository.py` +2, `backend/tests/test_ingest.py` +4(F-169 1·F-170 1·F-171 2). 검증: `cd project_code && python -m pytest backend/tests/` **157/157** · `python -m pytest siap/tests/ backend/tests/` **259/259** · `python -m pytest tools/tests/` **19/19** · `python tools/db_live_verify.py` **15/15** · `python project_docs/db/verify.py` **103/103**(불변) · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` **97/97** |
| 2026-08-10 | 단계 5 GPT 3차 재검증 | F-165~F-167(오류 3) 전량 수정, 기각 0 — 코드버그(F-165·F-166)→문서불일치(F-167) 순. **F-165(코드버그)**: F-161이 대입 별칭(`open_db = sql.connect`)을 잡도록 고쳤지만 `ast.Assign`만 순회해, 타입 주석이 붙은 대입(`open_db: object = sql.connect`, 문법상 별개 노드 `ast.AnnAssign`)은 여전히 놓쳐 FK-OFF 우회 연결도 11/11 거짓 통과했다 — `Assign`(다중 대입)과 `AnnAssign`(단일 대상, 값 없을 수 있음)을 `(targets, value)` 쌍으로 정규화해 같은 고정점 루프에서 함께 판정하도록 고쳤다. **F-166(코드버그)**: F-162가 `installed_at`에 `NOT NULL`+빈 문자열 차단은 걸었지만 형식은 전혀 보지 않아 `'not-a-date'`가 DB에도, `openapi.json`의 `format: date-time` 선언에도 그대로 통과했다 — JSON Schema 표준 자체가 `format`을 기본으로는 검증하지 않는 주석(annotation) 키워드라는 사양임을 확인한 뒤, 자체 평가기에 RFC 3339 정규식 지원을 추가하고 표준 구현 교차검증 쪽에도 `FormatChecker`를 명시로 붙여(안 붙이면 양쪽 다 통과시켜 "일치"로 위장된다) DDL에도 `GLOB` 최소 형식 검사를 추가했다. **F-167(문서불일치)**: F-160이 실측으로 찾은 스레드 버그(메인 스레드에서 연 연결을 `bind()`로 넘기면 `sqlite3.ProgrammingError`) 때문에 `_make_on_frame(db_path)`라는 지연 연결 래퍼를 새로 만들었는데, `run.py`·`siap/link.py` 머리말과 아키텍처 설계서 §3.1/§3.1-b/§4.2·Frame 구조 명세서 §5.1 다섯 곳은 여전히 F-160 이전의 `on_frame=backend.ingest.bind(conn)` 결선을 정본처럼 서술하고 있었다 — 문서대로 하면 첫 프레임에서 그대로 죽는다는 뜻이라 다섯 곳 모두 `_make_on_frame` 기준으로 재작성했다. **파급 정리**: F-166으로 DB 제약 테스트가 101→**103**종이 되며 `DB_스키마_설계서.md`(§0·§6.2, 분량 인용 45,000→50,513자)·`개발_착수_지시서.md`·`0937_요구사항_대조표.md`의 "101종" 인용을 갱신했고, `api_verify.py`의 반례 매트릭스는 47→**49**종으로 늘었다(Device 형식 2건). 검증: `pytest siap/tests/ backend/tests/` **253/253**(신규 2건, F-166) · `python contracts/test_contract.py` **62/62** · `python project_docs/db/verify.py` **103/103** · `python tools/db_live_verify.py` **11/11**(+ `tools/tests/test_db_live_verify.py` **10/10**, F-165 신규 2건) · `python project_docs/api/api_verify.py` **71/71**(매트릭스 49종, `FormatChecker` 포함 표준 구현 교차검증 일치) · `python project_docs/services/services_verify.py` **42/42** · `python project_docs/dev/dev_verify.py` **76/76** · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` 재확인 · `python tools/where.py` 단계 0~5 전부 `[OK]`, `현재 단계: 6`(services/·api.py) 재확인 |
| 2026-08-10 | 단계 5 GPT 재검증 | F-160~F-164(오류 5) 전량 수정, 기각 0 — 코드버그(F-160~F-162)→문서불일치(F-163·F-164) 순. **F-160(코드버그)**: F-154가 `on_frame` 훅을 마련했지만 실제 진입점 `run.py`의 세 실행 경로(`--mode simulate/replay/hardware`) 중 어느 하나도 `link.start(...)`에 `on_frame`을 넘기지 않아, 실제 실행에서는 DB에 아무것도 반영되지 않았다(§9.2 "2. DB 준비" 미이행) — `_prepare_db_path()`(스키마+시드)와 `_make_on_frame()`을 세 경로 모두에 연결했다. 고치는 과정에서 **새 버그를 실측으로 발견**했다: 메인 스레드에서 연 DB 연결을 그대로 넘기자 `python run.py --mode replay` 실제 실행에서 `sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread`로 죽었다(아키텍처 §4.1 "스레드별 연결" 위반) — `on_frame` 콜백이 SIAP I/O 스레드 안에서 처음 호출될 때 그 스레드 안에서 지연 연결하도록 다시 설계해 해소했다. **F-161(코드버그)**: F-155가 잡던 import 별칭에 더해, `open_db = sql.connect` 같은 **대입 별칭**은 여전히 놓쳐 FK-OFF 우회 연결도 11/11로 거짓 통과했다 — 대입 우변이 이미 알려진 모듈/함수 별칭인 단순 대입을 고정점까지 반복 수집하도록 확장해 대입 사슬(별칭의 별칭)까지 잡는다. **F-162(코드버그)**: F-158이 `installed_at TEXT NOT NULL`만 추가해 빈 문자열이 통과했고 `openapi.json`도 필드 생략을 허용했다 — DDL에 `CHECK(installed_at <> '')` 추가, API 스키마에 `required`+`minLength:1` 부여. **F-163(문서불일치)**: 표준 원문 문제가 아니라 F-154 반영 누락 — 아키텍처 §3.1·§3.1-a·§3.1-b·§4.2·§12, Frame 구조 명세서 §5.1, `contracts/siap_iface.py`의 `FrameBuilder` 독스트링 다섯 곳이 여전히 "회신은 `ingest.handle()` 반환값"이라는 F-154 이전 계약을 서술하고 있었다 — 전부 `_default_reply()`/`_dispatch()`/`on_frame` 부수효과 구조로 재작성했다. 재작성 중 `handle()` 의사코드에서 8.1.3.1~3 세 분기(F-079의 ems 배정)를 실수로 빠뜨려 `services_verify.py`의 F-079 회귀가 즉시 깨졌고, 그 자리에서 되살리며 "회신 RSC는 아직 SUCCESS 하드코딩이고 ems 판정을 반영하는 배치는 단계 6에서 다시 본다"는 미해결 지점을 주석으로 남겼다(새 결함을 감추지 않았다). **F-164(문서불일치)**: F-156이 만든 "장치상태가 정확히 1건일 때만 `operating_env`를 결속한다"는 표준 미규정 결정이 코드 주석에만 있고 CLAUDE.md §3.5 표·관련 설계 문서 어디에도 없었다 — §3.5에 결정 행을 신설하고 DB 스키마 설계서 §4·아키텍처 설계서 §4.4-a에도 같은 결정을 인용했다. **파급 정리**: F-162로 DB 제약 테스트가 100→**101**종이 되며 `DB_스키마_설계서.md`(§0·§6.2, 이번에 처음 잡힌 기존 98종 낙후분까지 함께 정정)·`개발_착수_지시서.md`·`0937_요구사항_대조표.md`의 "100종"·"98종" 인용이 즉시 낡아 갱신했고, `api_verify.py`의 반례 매트릭스가 43→**47**종으로 늘었다(Device 4건). F-163의 Frame 구조 명세서 분량 증가(11,000→13,043자)로 `개발_착수_지시서.md`의 분류 표기 인용도 함께 갱신했다(F-090/F-094 패턴, 매 라운드 반복되는 즉시 드리프트). 검증: `pytest siap/tests/ backend/tests/` **251/251**(신규 4건, F-160) · `python contracts/test_contract.py` **62/62** · `python project_docs/db/verify.py` **101/101** · `python tools/db_live_verify.py` **11/11**(+ `tools/tests/test_db_live_verify.py` **8/8**, F-161 신규 3건) · `python project_docs/api/api_verify.py` **71/71**(매트릭스 47종, 표준 구현 교차검증 일치) · `python project_docs/services/services_verify.py` **42/42**(F-079 회귀 포함) · `python project_docs/dev/dev_verify.py` **76/76** · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` **97/97** · `python tools/where.py` 단계 0~5 전부 `[OK]`, `현재 단계: 6`(services/·api.py) 재확인 |
| 2026-08-10 | 단계 5 GPT 검증 | F-154~F-159(오류 6) 전량 수정, 기각 0 — 코드버그(F-154~F-157)→문서불일치(F-158·F-159) 순. **F-154(코드버그)가 가장 무겁다** — `siap/link.py::_dispatch()`는 `on_frame`이 설정되면 `_default_reply()`를 완전히 대신했는데(F-137 이 그렇게 설계·테스트해 둔 지점), `backend/ingest.handle(frame, conn)`은 DB 스키마 설계서 §7대로 순수 DB 반영만 하고 회신을 만들지 않아 — 그대로 꽂으면 등록·ACK가 전부 사라졌다. "회신은 항상 `_default_reply()`가 만들고 `on_frame`은 부수효과로만 추가 호출된다(반환값 무시)"로 계약을 바꿔 `backend/`가 `siap/build.py`를 몰라도 되게 했다 — CLAUDE.md §3.4(표준 해석은 프로토콜 계층에만)를 어기지 않는 방향으로 해소한 것이며, F-137 테스트는 새 계약에서도 그대로 통과함을 확인(회귀 없음). `backend/ingest.py`에 `bind(conn)` 어댑터 신설. **F-155(코드버그)**: `db_live_verify.py`의 `sqlite3.connect()` 우회 탐지가 리터럴 이름만 봐서 `import sqlite3 as sql` 별칭 연결이 11/11 로 거짓 통과 — `tools/layer_verify.py`의 F-109 별칭 해석 원칙을 그대로 적용해 모듈/함수 별칭 바인딩을 먼저 모은 뒤 판정하도록 재작성. **F-156(코드버그)**: 표준 원문(7.1(10)·7.2.4.1) 대조 후 확인 — 같은 프레임에 장치상태·환경상태가 함께 와도 `operating_env` 관계를 만들지 않고 있었다. 장치상태가 정확히 1건일 때만(모호하지 않을 때만) 묶도록 `record_operating_env()` 신설 — 2건 이상이면 어느 것과 짝지어야 할지 프레임만으로 정할 수 없어(표준 미규정) 건너뛴다. **F-157(코드버그)**: 냉난방기가 같은 value 를 power·temperature 양쪽에 중복 기입해 CLAUDE.md §3.5 자신의 결정("주 필드에만, 나머지는 NULL")을 어기고 있었다 — FAN과 같은 패턴으로 power만 채우도록 수정. **F-158(문서불일치)**: 표준 원문(6.2.5) 대조 후 확인 — "장치설치정보에는... 설치일자..."가 요구사항인데 `schema.sql`·설계서·`models.py` 어디에도 없었다. `device_install_info.installed_at TEXT NOT NULL` 신설 → `project_docs/db/schema.sql`→`backend/schema.sql`→`DB_스키마_설계서.md` §8 순서로 동기(F-153 절차), `openapi.json`의 `Device` 스키마에도 노출(F-090 위반 해소). **F-159(문서불일치)**: 표준 원문(7.2.2.4) 대조 후 확인 — "모델명은... 전역 식별"이라면서 `device_info.model_name`에 `UNIQUE`가 없어 중복 삽입이 실제로 성공했다 — `UNIQUE` 추가(같은 3단계 동기 절차). F-158·F-159 를 합쳐 `project_docs/db/verify.py`에 검사 2건 신설(98→**100**), `backend/tests/test_schema_conformance.py`에 동일 이식, `device_install_info` 의 raw 위치지정 INSERT 4곳을 컬럼명 명시로 교체(F-024 원칙 재적용). **파급 정리**: 카운트가 98→100 으로 바뀌며 `개발_착수_지시서.md`·`0937_요구사항_대조표.md`의 "98종" 인용이 낡아 `dev_verify.py`(76→75)·`services_verify.py`(42→41)가 즉시 재발했고, F-158 의 신규 컬럼이 `api_verify.py`의 "테이블 컬럼 미노출 없음" 검사를 깨(71→70) 셋 다 같은 라운드에서 갱신해 해소했다(F-090/F-095 와 같은 즉시 드리프트 패턴, F-107 처리 때와 동일한 이유). **결함 주입 검증**: F-154(on_frame이 항상 None을 반환해도 노드가 정상 RES_SET_CONNECTION을 받는지 새 테스트로 확인), F-155(모듈 별칭·함수 별칭 PoC 파일을 실제 backend/에 주입해 FAIL 확인 후 제거해 11/11 복원). 검증: `pytest siap/tests/ backend/tests/ sim/tests/` **276/276**(신규 12건) · `python project_code/contracts/test_contract.py` **62/62** · `python project_docs/db/verify.py` **100/100** · `python tools/db_live_verify.py` **11/11** · `python project_docs/api/api_verify.py` **71/71** · `python project_docs/services/services_verify.py` **42/42** · `python project_docs/dev/dev_verify.py` **76/76** · `python tools/run_all.py` **15/15** · `python fix_log/meta_verify.py` **97/97** · `python tools/where.py` 단계 0~5 전부 `[OK]`, `현재 단계: 6`(services/·api.py) 재확인 |
| 2026-08-09 | 단계 4 3차 검증 | F-150~F-152(오류 2·치명 1) 처리 — 요건위반→코드버그 순. **F-150(오류)**: F-147 이 추가한 live 판정 검사는 `Frame.violations` 코드만 봐서, 판정을 결정하지 않는 자리(예: X06 마지막 바이트=Value LSB)가 변조돼도 통과했고 X02·X04·X08 은 검사 대상에도 없었다 — `mode_verify.py`에 `check_injection_actual_wire_bytes()` 신설, 디코더를 거치지 않는 맨 소켓으로 X01~X08 전량이 실제로 golden.jsonl 원본 바이트 그대로 나가는지 직접 캡처해 대조(8→**10항목**). **F-151(오류)**: F-146 이 재동기 게이트를 Version 단독으로 완화한 부작용 — 위반 프레임이 남긴 잔여 payload 바이트가 우연히 `0x12`면 그 지점을 새 헤더로 오인해 뒤따르는 정상 프레임을 삼켰다(오탐률 1/256) — `_resync_check()`에 **등록된 Node ID** 조건을 추가하되, Node ID 미등록 자체가 위반 목표인 X02 류를 위해 나머지 구조(resolve_kind·Transmission Type·element_count)가 자기충족적으로 유효하면 예외로 인정(오탐률을 4조건 수준 약 2⁻²²로 복귀). `CLAUDE.md` §3.5·`펌웨어_설계서.md` §5.7 갱신. **F-152(치명, 요건위반)**: F-148 기각 시 CLAUDE.md §7·아키텍처 §5.5 에만 "단계 4~7 예외"를 적어, CLAUDE.md §1-1 절대금지 행과 `GPT_검증_프롬프트.md` 단계 4 체크리스트("logs/ 에 하드코딩 센서값 있으면 실격")는 예외 없이 그대로 남아 판정 기준이 문서마다 갈린다는 재지적 — **판단 자체가 아니라 반영 범위가 문제였음을 인정**, 치명·요건위반 등급의 정책 결정이라 `AskUserQuestion`으로 사용자에게 처리 방향(①예외를 전 문서 반영 ②logs/·simulate 기본값에서 골든 재사용 전면 제거)을 확인받아 ①로 확정. CLAUDE.md §1-1 행에 예외 문구 추가, `GPT_검증_프롬프트.md` 체크리스트를 "random·math.sin 은 무조건 실격 / golden.jsonl 원본과 다른 하드코딩 값도 실격 / golden.jsonl 원본 그대로의 재사용은 예외"로 세분화. **부수 발견(자체)**: F-146·F-151 로 `펌웨어_설계서.md` §5.7 이 늘어 실측 분량이 46,000자→51,361자가 되면서 `dev_verify.py` 의 경로표 분량 대조(오차 10%)가 깨짐 — `개발_착수_지시서.md` 경로표를 52,000자(당시 46,000자 병기)로 갱신해 해소. **결함 주입 검증**: F-150(마지막 바이트 XOR 로 재현 스니펫 그대로 실행 → 신설 검사 정확히 실패), F-151(등록 Node ID 조건 제거 → 신규 테스트 2건 중 재현 시나리오 쪽이 정확히 `msg_id=[90,3072]`로 재현). **live 통합 검증**: S4-b 5종 + X02·X04·X08 을 한 세션에 연쇄 주입(몽타주 최악 케이스 가정) — 8/8 전부 목표 판정과 일치, X02(INVALID_NODE_ID)도 다른 위반 직후 연쇄 주입 시 유실 없이 분류됨을 별도 확인(수정 전에는 이 조건에서 X02 가 유실됨을 재현으로 확인). 검증: `pytest siap/tests/ backend/tests/ sim/tests/` **131/131**(신규 2건) · `python tools/mode_verify.py` 9→**10/10** · `python fix_log/meta_verify.py` **95/95** · `python project_docs/dev/dev_verify.py` 75→**76/76** · `python tools/where.py` 단계 0~4 전부 `[OK]`, `현재 단계: 5`(backend/) 재확인 |
| 2026-08-09 | 단계 4 2차 검증 | F-145~F-149(치명 2·오류 3) 처리 — 요건위반→코드버그 순. **F-145(치명)**: `virtual_node.py`가 101·102·103만 등록하는데 골든 X01~X08(X02 제외)은 전부 `Node ID=3`을 써서, live 주입 시 목표 판정 전에 `INVALID_NODE_ID`가 먼저 걸렸다 — Uno 흉내 노드 ID 를 101→**3**으로 변경, golden.jsonl 전수 대조 회귀 테스트 추가. **F-146(치명)**: `siap/codec.py`의 재동기 4조건이 `decode_frame()` 자신이 위반으로 보고하는 항목(resolve_kind·Transmission Type·element_count)과 겹쳐, 위반 프레임 하나를 낸 직후 뒤따르는 위반 프레임도 "노이즈"로 오인해 삼켜졌다(S4-b 5종 중 2종 유실) — 게이트웨이(Python) 재동기 조건을 Version 일치만으로 완화(F-146), 펌웨어(C)는 4조건 유지(§5.7 원문이 이미 구현별 차이를 전제). `CLAUDE.md` §3.5·`펌웨어_설계서.md` §5.7 갱신. **F-147(오류)**: `mode_verify.py`가 `inject.vector_bytes()`를 같은 파일에서 다시 읽은 값과만 비교해 실제 송신 함수·제어 채널·디코더 어느 것도 거치지 않았다 — 실제 simulate 링크로 S4-b 5종을 주입하고 `Frame.violations`를 대조하는 `check_injection_wire_path_and_classification()` 신설. **F-148(치명, 요건위반) — 기각**: `logs/`에 골든 벡터 재생 파일을 두는 것이 §1-1(합성 데이터 금지) 위반이라는 지적이었으나, `개발_착수_지시서.md:242`(단계 4 "하지 않을 것")가 "`logs/`는 단계 8의 실측 캡처로 채운다. 그전까지는 골든 벡터만 재생한다"고 이미 명시적으로 허용한 경로였다 — 근거를 들어 기각하되, 이 결정이 `아키텍처_설계서.md` §5.5(원래 "실측 로그의 값 분포 재사용"이라고만 적혀 있어 실제 코드와 어긋나 있었다)와 `CLAUDE.md` §7 "실측 로그" 행에는 정식으로 반영돼 있지 않았던 문서화 공백을 발견해 이번에 메웠다. **F-149(오류)**: F-144(직전 라운드 자체 발견) 수정은 적용됐지만 어떤 소스에도 "F-144" 문자열이 없어 `meta_verify.py`의 회귀 테스트 존재 검사가 94/95로 실패 — `tools/tests/test_where.py`에 회귀 테스트 추가. **결함 주입 검증**: 5건 모두 수정 전 상태로 되돌려 새/기존 검사가 정확히 실패하는 것을 확인한 뒤 복원 — F-145(node_id 101 복원 → 신규 테스트 2건 실패), F-146(4조건 복원 → `msg_id=[50]`만 남고 52·54 소실 재현), F-147(Version 바이트 변조 송신 → 새 검사 실패로 잡힘; F-145 원인 재도입 → 8/9 실패), F-149(check_stage_0 구버전 복원 → 신규 테스트 실패). **live 통합 검증**: 실제 `VirtualNodeServer`+`SiapNodeLink` simulate 링크에서 시연 시나리오 §3.1 S4-b 순서(X01→X03→X05→X06→X07, 0.3초 간격)를 그대로 재현 — 5/5 전부 목표 판정(`INVALID_VERSION`·`INVALID_FORMAT`·`INVALID_TRANSMISSION_TYPE`·`INVALID_DATA_TYPE`·`INVALID_DATA_SUBTYPE`)과 일치 확인. 검증: `pytest siap/tests/ sim/tests/` 전량 재통과(신규 2건 포함) · `python tools/mode_verify.py` 8→**9/9** · `python fix_log/meta_verify.py` 94(가상)→**95/95** · `python tools/where.py` 단계 0~4 전부 `[OK]`, `현재 단계: 5`(backend/) 재확인 |
| 2026-08-09 | 단계 4 개발 | `sim/`·전송 계층 신설(개발 착수 지시서 §3.6) — `sim/_wire.py`(독립 비트 인코더, `siap/codec.py` 미재사용), `sim/virtual_node.py`(가상 노드 3+1대, `simulate` TCP 서버), `sim/replayer.py`(`replay` TCP 서버, F-042 `dir="rx"` 필터), `sim/inject.py`(골든 위반 8종 주입, `X01`~`X08` 외 거부), `sim/golden_log.py`(실측 로그 없는 단계의 대체 재생 로그 생성기) 신설. `run.py` 를 스켈레톤에서 실동작으로 교체(§9.2 의 "모드 스레드 기동"·"SIAP I/O 스레드 기동"만 채움 — DB 준비·uvicorn 은 단계 5·6). 신설 검증기 `tools/mode_verify.py` 4항목(외부 도구 0, `dir="rx"` 필터, 주입 8종 골든 일치, loopback 전용). **센서값 출처 결정(사용자 확인, 2026-08-09)**: 아키텍처 설계서 §5.5 가 요구하는 "실측 로그의 값 분포"가 실제로는 단계 8 에서만 생기는 산출물이라 단계 4 시점에 순환 의존이었다 — 골든 벡터(`judgement=normal`) 의 `DEVICE_MAIN_INFO.Value` 를 실측 대용으로 재사용하기로 결정하고 `virtual_node.py`·아키텍처 설계서 §5.5 에 근거를 남겼다. **F-144(자체 발견)**: 단계 4 를 실제로 완료하자 `tools/where.py::check_stage_0()` 이 `run.py --mode simulate` 출력에 `"미구현"` 문구가 있어야 통과로 판정해(단계 0 시점의 스냅샷 요구), `check_stage_4()` 의 정반대 요구("단계 4부터는 '미구현' 문구가 사라져야 한다")와 충돌 — 단계 1~4 가 전부 통과함에도 `현재 단계: 0` 으로 후퇴 보고했다. 정상 종료(exit 0)만 보도록 좁혀 해소. 이 과정에서 `sim/virtual_node.py`·`sim/tests/test_virtual_node.py` 의 "random/math.sin 을 안 쓴다"는 설명 주석이 `meta_verify.py` 의 합성 데이터 금지 스캐너(문자열 매칭, 부정문 이해 못함)를 오탐시키고, `sim/tests/test_inject.py` 의 예시 문자열 `"../../etc/passwd"` 가 비밀정보 스캐너를 오탐시킨 것도 함께 발견해 문구를 조정했다(별도 fix_log 항목 없음 — 기존 코드가 아니라 이번에 새로 쓴 코드 자신의 표현 문제). 검증: 통합 스모크(가상 노드 서버 + `SiapNodeLink` 실제 TCP 왕복, 노드 3+1대 등록·`inject.py` CLI 로 X01 주입 후 `INVALID_VERSION` 위반 검출 확인, `replayer.py` 로 골든 파생 로그 재생) 전부 실측 실행으로 확인 — **재현 도중 `Replayer` 가 마지막 프레임 전송 직후 바로 `close()` 하면 실전(speed=1.0) 환경에서 마지막 레코드가 간헐적으로 유실됨을 5회 반복 실행으로 발견**, 전송 후 0.2초 지연을 두어 해소(5/5 안정 재현). `pytest siap/tests/ backend/tests/ sim/tests/` **127/127**(신규 29건) · `python project_code/contracts/test_contract.py` **62/62** · `python tools/run_all.py` 13 → **14/14**(`mode_verify.py` 신규 자동 편입, F-094 원칙대로 이름 목록에 손대지 않음) · `python fix_log/meta_verify.py` 93 → **95/95**(위 오탐 2건 해소로 순증) · `python tools/where.py` 재실행 결과 단계 0~4 전부 통과, `현재 단계: 5`(backend/, 실제 미착수)로 정확히 전진 확인 |
| 2026-08-09 | 단계 3 2차 검증 | F-141 ~ F-143 (3건) 전량 수정, 기각 0. **셋 다 F-135~F-140 처리 직후의 재검증에서 나왔고, 그중 F-141 은 F-140(Python `codec.py`)과 정확히 대칭인 결함이 C `siap_frame.c` 에도 있었다는 재확인이다.** F-141 은 `handle_header_complete()` 가 헤더 위반 5종(Version·resolve_kind 실패·element_count<0·Transmission Type·미등록 Node ID) 전부에서, 아직 구조가 확인되지 않은 위반 헤더의 `payload_len` 을 그대로 `begin_drain()` 에 넘겨 그 수만큼 **미래에 도착하는** 바이트를 무조건 폐기하는 지점이었다 — Python 은 이미 버퍼에 있는 바이트를 즉시 삼켰지만(F-140), C 는 스트리밍이라 아직 도착하지 않은 바이트를 삼킨다는 차이만 있을 뿐 원인은 같다. 5개 호출부를 `begin_drain(d, 0)` 으로 바꿔 즉시 §5.7 재동기로 넘어가게 하고, `S_FIXED`/`S_ELEM` 단계 위반(이미 구조가 확인된 뒤라 `payload_len` 을 신뢰할 수 있는 경우)의 `begin_drain(d, remaining)` 은 그대로 두어 두 경우를 구분했다. **`펌웨어_설계서.md` §5.1 상태도·§5.7 재동기 규칙 자체가 옛 동작("헤더 위반 → plen 만큼 버림")을 그대로 규정하고 있어 설계 문서도 함께 정정했다** — 코드만 고치고 설계서를 그대로 두면 이 결함이 "의도된 설계"로 보여 재발한다. 기존 `case_resync()`(RS1/RS3)는 위반 헤더의 payload_len 이 우연히 0이라 이 결함을 전혀 가리지 못했다는 것도 확인해, payload_len=12(뒤이은 정상 ACK 와 우연히 같은 길이)로 재구성한 신규 케이스를 별도로 추가했다. F-142 는 `tools/xcodec_verify.py::_build_and_run_c_dump()` 가 `subprocess.run()` 의 `returncode` 를 받아 두고도 어디서도 검사하지 않아, C 덤프가 assertion·sanitizer 등으로 비정상 종료해도 죽기 전까지 낸 출력만 완전하면 8/8 로 통과하던 지점이다 — 반환값에 `returncode` 를 추가하고 내용과 무관하게 종료코드 자체를 판정하는 검사를 맨 앞에 추가했다(8종 → **9종**). F-143 은 F-138 회귀 테스트(`test_send_bounded_even_when_read_always_errors_f138`) 자신의 assertion 이 계약 상한(2초)의 2배(`elapsed < 4.0`)를 허용해, F-138 의 계약이 다시 깨져도 잡지 못하던 지점이다 — **처리 도중 같은 파일의 자매 테스트(`test_send_times_out_when_no_response`, 상한 2초에 여유 5.0초)도 동일한 결함을 갖고 있음을 자체 발견해 함께 좁혔다**(보고서는 한 곳만 지목). 두 assertion 을 하드코딩된 여유 대신 계약값에서 유도한 상한 + 스케줄링 여유 0.5초로 교체했다. 검증: F-141 은 5개 호출부를 전부 옛 동작으로 되돌린 뒤 재빌드해 신설 검사 2건만 정확히 FAIL(148→146/148)함을 확인 후 원복, 재빌드로 148/148 재확인. F-142 는 `subprocess.run` 을 런타임에 감싸 `dump_golden` 실행 결과의 `returncode` 만 7로 강제하는 방식으로 보고된 재현을 재구성해 신설 검사가 FAIL(8/9)하고 검증기 자체 종료코드도 0→1 로 바뀜을 확인. F-143 은 `SiapNodeLink.send` 를 런타임에 감싸 반환 직전 1초를 더 대기시켜 두 테스트가 정확히 FAIL(elapsed 3.0s대 vs 상한 2.5s)함을 확인 — F-142·F-143 은 저장소 파일을 건드리지 않는 런타임 몽키패치라 별도 원복이 필요 없었다. 회귀: `make && ./test_bitpack && ./test_siap_frame && ./test_status_codes && ./test_golden && ./test_node_state` 41/**148**/53/253/91(143→148) · `python project_code/contracts/test_contract.py` **62/62** · `python -m pytest siap/tests/ backend/tests/` **98/98** · `python tools/xcodec_verify.py` 8 → **9/9** · `python tools/run_all.py` **13/13** · `python fix_log/meta_verify.py` **93/93** · 실행파일 잔존 0 · §1 금지 패턴 스캔 클린 |
| 2026-08-08 | 단계 2b 1차 검증 | F-114 재발(신규 ID 없음, siap_frame.h 로 옮겨간 SIAP_WUR 9개가 check_wur 사각지대) + F-116 ~ F-120 (5건) 전량 수정, 기각 0. **F-116(B02)이 가장 무겁다 — `golden_layout.py` 의 B02 항목이 note 에는 "element_count() 가 None 을 돌려 INVALID_FORMAT 이 된다"고 정확히 적어 놓고도 `violations=` 인자가 빠져 `judgement=normal`로 생성됐다.** `VIO()` 정의를 `V()` 바로 뒤로 옮기고 B02 에 `violations=VIO(0x09,"INVALID_FORMAT","7.3.1",...)` 를 추가, `golden_layout.py` 재실행으로 두 JSONL 을 정본에서 재생성했다(직접 편집 없음). `golden_verify.py` 는 판정 분류(violation 7→8→9, normal 44→43)를 갱신하고, "n is None 인 벡터는 normal/alert 일 수 없다"는 의미 검사와 `derive()`(바이트 독립 재판정)에 "고정부 없이 가변부만 있는 메시지의 rest==0"·"N>16 상한" 두 조건을 신설했다. **F-119 처리 중 실제 C↔Python 분기 불일치를 발견했다** — `contracts/frame.py::resolve_kind()` 는 후보가 하나뿐이면 `element_count()` 를 전혀 안 보고 그 kind 를 확정하는데(단일 후보는 "코드가 무엇을 뜻하는가"만 결정), 기존 C `siap_resolve_kind()` 는 후보 수와 무관하게 항상 `element_count()` 를 안에서 검사해 B02 류를 **다른 지점**에서 걸렀다(최종 위반 코드/조항은 우연히 같았지만 펌웨어 설계서 §5.3 "Python 원본과 분기 구조가 같아야 한다"를 어김). `siap_resolve_kind()` 를 Python 과 동일하게 재작성(단일 후보 즉시 확정 / 다중 후보만 `element_count()` 루프)하고, `handle_header_complete()` 에 단일 후보 확정 후의 별도 `element_count()` 재검사를 추가했다 — 이 재작성으로 `test_siap_frame.c` 의 RK6(extended 0x0801) 테스트도 Python 실제 동작에 맞춰 정정했다. F-119 본체는 `test_golden.c` 에 `kind_from_str()`(34종 전량)을 신설하고, FSM 콜백이 넘긴 값이 아니라 헤더 바이트에서 직접 부른 `siap_resolve_kind()`/`siap_element_count()` 결과를 골든의 기대 `kind`/`n` 과 독립 대조하도록 했다. F-117 은 `check_stage_2b()` 가 (F-098 이 고친 2c 와 달리) `make` 를 스스로 부르지 않고 기존 바이너리만 확인해, 선행하는 `check_stage_2a()` 의 `make clean`(2b 타깃도 포함하도록 이번에 넓힌 CLEAN_FILES) 이 2b 산출물을 지워버리는 구조적 미통과였다 — `check_stage_2a()` 와 동일한 자체 빌드+`try/finally` 정리 패턴으로 재작성하고, "빌드 실패"(컴파일 로그)와 "실행 파일 없음"(Windows 확장자 자동 탐색에 기댄 `_run()` 직접 실행, `.exists()` 선검사 금지)을 구분했다. F-118 은 `#include/**/<Arduino.h>` 처럼 주석으로 감싼 include 가 원문 정규식을 우회하는 지점으로, (a) 주석·줄 이어쓰기를 정규화한 텍스트 재스캔과 (b) `gcc -E` 실제 전처리 결과(독립 입력, 전처리 실패 메시지에서 금지 헤더 흔적 추출 포함) 두 겹으로 다시 짰다. F-120 은 `siap_element_count()` 가 나눗셈 후 N=0 만 보고 `SIAP_MAX_DEVICES_PER_NODE`(16) 초과를 안 봐서 N=17 프레임이 정상으로 통과하던 지점이다 — CLAUDE.md §5 절차(근거: CLAUDE.md §3.5/F-064, 2026-08-08 사용자 승인)를 밟아 `contracts/frame.py::element_count()` 에 동일한 상한을 추가하고(Python↔C 정책 위치 일치), 골든에 B11(REQ_SET_DEVICE_CONTROL N=17 거부)을 신설해 재생성했다(52→53건, 경계 10→11). CLAUDE.md·`개발_착수_지시서.md`·`web_verify.py` 의 벡터 건수·검사 건수 인용도 함께 갱신했다. 검증: F-114 재발은 `siap_tx_put_hdr` 의 `SIAP_WUR` 를 실제로 제거해 신설 스니펫 9종 중 정확히 그 하나만 `FAIL(F-113)` 로 검출됨을 확인 후 원상복구. F-116~F-120 은 각각 결함을 실제로 재주입(B02 판정 되돌리기, `where.py` 단계 2a→2b 순차 실행 중 make 없이 실행, `Arduino.h` 주석 포장 파일 삽입, `resolve_kind` 오매핑 하드코딩, N=17 프레임 프로브)해 신설/수정된 검사가 정확히 그 결함만 잡고 정상 상태에서는 통과함을 확인 후 원상복구. 회귀: `make && ./test_bitpack && ./test_siap_frame && ./test_status_codes && ./test_golden` 41/41·104/104·53/53·**253/253**(52→53건) · `python project_code/contracts/test_contract.py` **62/62** · `python project_docs/contracts/vectors/golden_verify.py` 29 → **31/31** · `python tools/core_purity_verify.py` **6/6** · `python tools/where.py` 단계 2b 통과 · `python tools/run_all.py` **12/12** · `python fix_log/meta_verify.py` **88/88** |
| 2026-08-08 | 단계 2a 2차 검증 | F-111 재수정(신규 ID 없음) + F-114 ~ F-115 (2건) 신규 수정, 기각 0. **F-111 은 직전 라운드의 수정이 절반짜리였다는 재확인이다 — `make clean` 자체는 고쳤지만, "출구 명령을 문서 그대로 실행하면 그 다음에 뭘 해야 clean 이 불리는가"는 아무도 답하지 않았다.** `make test_bitpack && ./test_bitpack` 을 그대로 실행하면 `test_bitpack.exe` 가 여전히 남고, 그 직후 `offline_verify.py`(10/11 → 실행파일 검출)·`run_all.py`(10/11)·`meta_verify.py`(84/86)가 깨지며, `where.py` 는 자신이 만든 잔여물 때문에 **두 번째 실행에서 단계 0으로 후퇴**하는 자기 오염까지 있었다 — 근본 수정은 `tools/offline_verify.py` 의 `check_no_binaries()` 가 스캔 직전 `project_code/firmware/` 아래 모든 Makefile 에 대해 `make clean` 을 먼저 시도하도록 바꾼 것이다("알려진 빌드 산출물은 검증기 자신이 치우고, 그래도 남는 낯선 실행파일만 위반으로 본다" — 실행파일 판정 기준을 낮추는 게 아니라 정상적 개발 잔여물과 진짜 위반을 가르는 것). `tools/where.py` 의 `check_stage_2a`·`_rebuild_and_run_all_tests`(2c 용)도 각각 `try/finally` 로 자기 정리를 추가해 이중 방어했다. F-114 는 `check_wur.py` 가 "컴파일이 0이 아닌 코드로 끝났다"만 보고 **왜** 실패했는지는 안 봐서, 스니펫 파일을 지우거나(No such file) 헤더를 깨도(무관한 오류) 똑같이 `OK(F-113)` 을 출력하던 지점이다 — 컴파일러 자체가 실행 가능한지 선검사하고, 스니펫이 실재하는지 확인하고, 실패했다면 그 stderr/stdout 진단문에 `unused-result`/`warn_unused_result` 가 실제로 있는지까지 확인하도록 다시 짰다. F-115 는 F-112 가 추가한 비정렬 9bit 케이스가 왕복(같은 구현의 write→read)과 "0이 아니다"만 봐서, writer 와 reader 를 **함께** 대칭으로 틀리게(LSB-first) 바꿔도 서로 역연산이라 안 걸리던 지점이다 — CLAUDE.md §0 의 "하드웨어 없이 표준 준수를 검증할 수 있다" 주장을 직접 반증하는 종류의 결함이었다. bitpos·nbits 로부터 **손이 아니라 독립 Python 계산**으로 뽑은 기대 바이트열(9bit: `06 94 00 00`, 겸사겸사 20bit 케이스도 `01 4B 86 00`)을 `memcmp` 로 직접 대조하도록 두 케이스 모두에 추가했다. 검증: F-111 은 clean 을 한 번도 안 부르고 "빌드→실행→offline_verify.py→run_all.py→meta_verify.py→where.py(3연속)" 를 그대로 실행해 전부 통과·단계 판정 불변(2b 고정) 확인. F-114 는 스니펫 파일명을 존재하지 않는 이름으로, 컴파일러를 존재하지 않는 이름으로, 반환값 폐기와 무관한 구문 오류로 각각 주입해 셋 다 `FAIL(F-114)` 로 정확히 분류됨을 확인. F-115 는 `bp_write`/`bp_read` 의 9bit 분기만 LSB-first 로 대칭 주입해 왕복 검사는 그대로 통과하되 신설 바이트 대조만 FAIL(40/41)함을 확인 후 원상복구. 회귀: `make test_bitpack && ./test_bitpack` 39 → **41/41** · `python tools/offline_verify.py` clean 미호출 상태에서도 전체 통과 · `python tools/run_all.py` **11/11** · `python fix_log/meta_verify.py` **86/86** · `python tools/where.py` 3연속 실행 모두 단계 2b 로 일관 |
| 2026-08-08 | 단계 2a 검증 | F-111 ~ F-113 (3건) 전량 수정, 기각 0. **셋 다 "개발 컴퓨터(Windows)에서는 통과하지만 문서화된 절차를 그대로 따르면 깨진다"는 한 형태다 — 코드 자체의 논리 결함이 아니라 빌드·검증 파이프라인의 플랫폼 가정이 깨지는 지점이었다.** F-111 은 `-o test_bitpack` 로 지정해도 MinGW GCC 가 실제로 `test_bitpack.exe` 를 만들어, 그 파일이 `tools/offline_verify.py`의 실행파일 스캔·`tools/run_all.py`·`tools/where.py` 단계 판정을 전부 무너뜨리는 지점이었다 — `Makefile` 의 `clean` 이 두 이름을 모두 지우도록 고쳤다. F-112 는 `test_bitpack.c` 가 20bit·14bit 는 비정렬 경계로 시험하면서 9bit 은 전 폭 스윕(항상 bitpos=0 에서 시작, 즉 정렬된 자리)에만 있어 "비정렬 9bit 은 조용히 쓰지 않고 성공한 척"하는 결함이 안 걸렸고, 20bit 초과값·전 폭 스윕의 초과값 거부는 `bitpos` 불변만 보고 버퍼 전체 불변은 안 봐서 "거부하면서 몰래 1비트를 건드리는" 결함도 안 걸렸다 — 비정렬 9bit 전용 케이스와, 두 초과값 경로의 버퍼 스냅샷 비교를 추가했다. F-113 은 `test_bitpack.c` 의 모든 호출이 반환값을 실제로 쓰기 때문에 `SIAP_WUR` 가 헤더에서 통째로 빠져도(또는 `-Werror=unused-result` 가 빠져도) 자동 출구가 그 사실을 스스로 드러낼 수 없던 지점이다 — "컴파일 실패가 성공"인 별도 스니펫 둘(`bp_write`·`bp_write_f32` 각각)을 두고 `test_bitpack` 의 선행조건으로 걸었다. **이 과정에서 셸 문법(if/then/fi, 리다이렉션)으로 짠 `check_wur` 레시피가 `tools/where.py` 가 PowerShell 에서 `make` 를 부를 때(POSIX sh 를 못 찾아 `mingw32-make` 가 cmd.exe 로 떨어지는 경로)만 깨지는 것을 추가로 발견했다** — `check_wur.py`·`clean.py` 로 옮겨 Make 레시피를 "python 스크립트 인자..." 한 줄로만 두어 셸이 무엇이든 동일하게 동작하도록 고쳤고, 그 과정에서 `subprocess.run(text=True)` 가 gcc 진단의 UTF-8 문자(소스 주석의 em dash)를 로케일 기본(cp949)으로 디코딩하려다 죽는 것도 함께 잡아 명시적 `encoding="utf-8"` 로 고쳤다(F-045·F-096·F-102 와 같은 부류). 검증: F-111 은 F-111 자신이 지정한 순서(단계 2a 출구 → `offline_verify.py` → `run_all.py` → `meta_verify.py` → `where.py`)대로 재실행해 전부 통과 확인. F-112 는 두 반례(비정렬 9bit 무기록, 20bit 초과값 시 `buf[0]` 오염)를 각각 `bitpack.c` 에 실제 주입해 34/34→37/39·39/39 아님을 확인 후 원상복구. F-113 은 `bp_write`·`bp_write_f32` 각각의 `SIAP_WUR` 를 실제로 제거해 `make test_bitpack` 이 정확히 `check_wur` 단계에서 실패(exit 2)함을 Git Bash·PowerShell 양쪽에서 확인 후 원상복구. **부수 발견 — `meta_verify.py` §5 "수정완료 코드버그에 대응 회귀 테스트 존재" 검사가 `project_docs/**/*_verify.py` 명명 규칙에 고정돼 있어 이번 셋의 회귀 근거(C·Makefile·`check_wur.py`/`clean.py`)를 못 찾고 F-111~F-113 을 다시 FAIL 로 띄웠다 — F-094 "목록 고정 금지" 원칙대로 스캔 대상을 이름 목록에서 `project_code/`·`tools/` 두 디렉터리 전수(.py/.c/.h/Makefile)로 넓혀 해소했다(별도 ID 없이 이 라운드 처리에 포함 — F-111~F-113 자신의 검증 요구를 닫기 위한 직접적 필요에서 나온 수정이라서다).** 회귀: `make test_bitpack && ./test_bitpack` 34 → **39/39** · `python tools/run_all.py` **11/11** · `python fix_log/meta_verify.py` **86/86**(스캔 확장 후 F-111~F-113 전부 검출) · `python tools/where.py` 단계 2a 통과·현재 단계 2b 판정 |
| 2026-08-08 | 단계 1 재검증 | F-109 ~ F-110 (2건) 전량 수정, 기각 0. **둘 다 직전 라운드(F-105·F-106)가 남긴 "잡을 수 있는 것만 잡았다"는 빈틈이다 — 같은 회귀 항목이 여전히 통과하는 채로 표기·시그니처만 바꾼 반례다.** F-109 는 `layer_verify.py`가 ① `import project_code.siap.codec`처럼 저장소 루트가 `sys.path`에 있는 실행 방식(패키지 접두어)과 ② `from importlib import import_module as load; load("siap.codec")`처럼 동적 import 함수를 별칭으로 들여온 경우를 놓친 지점이다 — `_effective_top()`(첫 구성요소가 `PROJECT_CODE.name`이면 다음 구성요소를 실질 최상위 이름으로 취급)과 `_dynamic_import_aliases()`(`from importlib import import_module as X`로 지역에 들어온 이름 전부를 추적)를 신설해 `import`·`from`·동적 호출 세 경로 전부에 적용했다. F-110 은 `test_contract.py`의 F-106 회귀가 "메서드가 존재하고 최소 입력으로 호출된다"까지만 보고, `start`에서 `**opts`를, `send`에서 `timeout`을 빼거나 `recv()`가 list를 그대로 반환해도 60/60이던 지점이다 — `inspect.signature`로 `SiapLink` Protocol과 구현의 파라미터 이름·kind·기본값을 대조하는 `_sig_compat()`을 추가하고, `start(..., 임의 키워드)`·`send(frame, timeout=2.0)` 실호출과 `recv()`의 `iter(x) is x` Iterator 계약 검사를 더했다(62건). 검증: F-109 는 4변형(qualified import·qualified from-import·별칭 동적 import·정상 상대 import 오탐 없음)을 실제 파일 주입으로 재현 후 반전 확인, F-110 은 시그니처 결손 Fake(`start`에 `**opts` 없음·`send`에 `timeout` 없음·`recv()`가 `list` 반환)를 `fake_link.py`에 실제로 주입해 3항목 모두 FAIL 확인 후 복원. F-109 회귀 5건은 F-105 자리 옆에 `meta_verify.py`에 영구 고정. 회귀: `python project_code/contracts/test_contract.py` 60 → **62/62** · `python tools/layer_verify.py` **7/7** 유지 · `python project_docs/dev/dev_verify.py` **76/76**(§3.1 인용 62/62 갱신 포함) · `python tools/run_all.py` **11/11** · `python fix_log/meta_verify.py` 81 → **86/86**(F-109 반례 5건 추가) · `python tools/where.py` 단계 1 판정 불변 |
| 2026-08-08 | 단계 1 검증 | F-105 ~ F-108 (4건) 전량 수정, 기각 0. **F-105·F-106 이 한 형태다 — 단계 1에서 신설한 검증기 자신이, 자신이 지키려던 경계를 실제로는 지키지 못했다.** F-105 는 `layer_verify.py`의 `_top_level_modules()`가 `ImportFrom.level > 0`(상대 import)을 전부 "같은 패키지 내부"로 간주해 건너뛴 지점이다 — `from ..siap import codec`처럼 상위로 벗어나는 상대 import 는 실제로 금지 계층을 참조하는데도 7/7 로 통과했고, `importlib.import_module("siap.codec")` 같은 동적 import 는 AST 에 `Import`/`ImportFrom` 노드 자체가 없어 애초에 탐지 대상이 아니었다 — 파일의 패키지 위치(`PROJECT_CODE` 기준 dotted path)를 계산해 상대 import 를 절대 경로로 되돌리고(레벨이 패키지 깊이를 넘으면 보수적으로 위반 쪽으로 판정), `importlib.import_module`/`__import__` 호출의 문자열 리터럴 인자를 정적으로 검사하도록 고쳤다. F-106 은 `contracts/fake_link.py`(단계 1 신설 산출물)를 어떤 출구도 실제로 import·호출하지 않아, 빈 클래스나 문법 오류로 바꿔도 계약 검증 56/56·계층 검증 7/7 이 그대로 통과하던 지점이다 — `test_contract.py`에 `FakeSiapLink`를 실제 로드→인스턴스화→`SiapLink` 메서드 7종을 최소 정상 입력으로 호출해 반환형까지 확인하는 검사 4건을 추가했다(56→**60**). F-108 은 `meta_verify.py`가 `tools/*_verify.py` 전부에 동일한 180초 CP949 직접실행 제한을 걸어, 6조합 실제 설치를 수행하는 `offline_verify.py`(정상 소요 ~179초)가 사소한 부하만으로 `TimeoutExpired`가 나 "CP949 결함"과 "단순 시간 부족"을 구분할 수 없던 지점이다 — 스크립트별 제한을 분리하고(`offline_verify.py` 600초), 시간 초과와 비정상 종료를 다른 상태로 분류하며, 합성 슬로우 스크립트로 분류 로직 자체의 정상 대조군 회귀 2건을 추가했다. F-107 은 `Frame_구조_명세서.md`가 §머리말·§6 을 53/53(F-086 반영 전 수치)으로, §5.1 게이트웨이발 빌더 목록에서 `set_device_property`(0943 8.1.3.2, 표 7-2 — 양방향 규정)를, §7 "다음 단계"에서 이미 완료된 `fake_link.py`·`golden.jsonl` 이관을 남겨 둔 문서불일치로, 구현·0943 원문 대조 결과 문서가 틀려 정정했다(53/53 → 이번 라운드 실측 반영 **60/60**, `set_device_property` 추가, 완료 항목을 "완료" 절로 분리). 이 과정에서 F-106 이 `test_contract.py` 를 56→60 으로 늘려 `개발_착수_지시서.md` §3.1 출구 인용도 56/56 으로 낡아진 것을 같은 자리에서 함께 잡아 60/60 으로 갱신했다(F-090 과 같은 부류의 재발이나, 이번엔 직전 수정이 유발한 즉시 드리프트라 같은 라운드에서 처리). 검증: 세 반례(상대 import·동적 import·정상 상대 import 오탐 없음) 및 F-106 두 반례(빈 클래스·문법 오류) 전부 결함 주입으로 재현 후 수정으로 반전 확인. `layer_verify.py`는 검사 본문이 모듈 최상위에서 `sys.exit()`까지 호출해 `import`만으로 프로세스가 죽는 구조였음이 F-105 회귀를 meta_verify.py 에 넣는 과정에서 추가로 드러나, `where.py`·`offline_verify.py`와 같은 `main()`+`__main__` 가드로 함께 고쳤다(별도 fix_log 항목 없이 F-105 처리에 포함 — 근본 원인이 같은 파일의 같은 리팩터링 지점). 회귀: `python project_code/contracts/test_contract.py` 56 → **60/60** · `python tools/layer_verify.py` **7/7** 유지(가드 리팩터링 후에도 단독 실행 동일) · `python project_docs/dev/dev_verify.py` **76/76** · `python tools/run_all.py` **11/11** · `python fix_log/meta_verify.py` 75 → **81/81**(F-108 정상 대조군 2건 + F-105 반례 4건 추가) · `python tools/where.py` 단계 1 판정 불변 |
| 2026-08-08 | 단계 0 재검증 (2회차) | F-102 ~ F-104 (3건) 전량 수정, 기각 0. **직전 라운드가 고친 `tools/offline_verify.py`·`tools/where.py`를 다시 공격해 나온 결과라 전부 "직전 수정의 빈틈" 형태다.** F-102는 F-097 수정 때 상세 문구에 넣은 em dash(U+2014)가 CP949 콘솔에서 검증기 자체를 죽였고, `offline_verify.py`에는 다른 `*_verify.py`들이 다 가진 `sys.stdout.reconfigure` 2중 방어 가드조차 없었다 — 가드를 추가하고 em dash를 걷어냈는데, 같은 패턴(가드 부재+em dash)이 `where.py`·`run_all.py`에도 잠복해 있어 함께 고쳤다. F-103은 F-097이 "이름만 파싱"으로 4번째 의존성은 잡았지만 `>=`·`~=` 같은 느슨한 버전 지정자, 배포명당 중복 버전 휠, `.gitignore`의 `!` 재포함(negation)까지는 보지 못한 지점을 다뤘다 — 연산자·버전까지 파싱해 `==`만 허용하고, 휠 파일명에서 배포명·버전을 뽑아 버전 유일성을 강제하고, 자체 fnmatch 매칭을 폐기하고 임시 git 저장소의 `git check-ignore`로 실제 gitignore 의미론에 위임했다(CLAUDE.md §6.2 F-095 "표준 구현과 교차 검증" 원칙 적용). F-104는 F-098이 avr-size를 "실행은 하되 종료 코드만 보고 값은 안 봄" 상태로 남긴 지점으로, `text/data/bss`를 파싱해 펌웨어 설계서 §3.1·§3.4·개발_착수_지시서 §1.5의 "SRAM 40% 이하" 예산과 실제로 비교하도록 고쳤다(ESP32는 §3.1이 이미 "제약 아님"으로 판정을 끝낸 대상이라 예산 항목 자체를 만들지 않음). 검증: 3건 전부 제안된 반례로 재현 후 수정으로 반전 확인(999,999,999B 예산 초과 차단, `!.omc/` 재포함 차단, `click` 중복 버전 차단, CP949 강제 실행 exit 0). 회귀: `fix_log/meta_verify.py`에 F-102(cp949 강제 직접 실행 + tools/ 전체 print() 리터럴 AST 정적 검사)·F-103(4종)·F-104(2종) 추가, 55 → **73/73**. 기존 `tools/offline_verify.py` 6조합 오프라인 설치 포함 전체 통과 · `tools/where.py` 단계 1 판정 불변 · `tools/run_all.py` **10/10** · `python project_docs/dev/dev_verify.py` **76/76** 유지 |
| 2026-08-08 | 단계 0 독립 검증 | F-097 ~ F-101 (5건) 전량 수정, 기각 0. 전부 **개발 착수 지시서 §3.0(단계 0) 신설 검증기 자신의 결함**이라는 한 형태다 — 어제 라운드(F-096)와 같은 부류가 이번엔 `tools/` 세 스크립트에서 나왔다. F-097 은 `offline_verify.py`의 의존성·`.gitignore` 검사가 각각 "휠 파일명만 봄"·"부분 문자열 포함"이라 몰래 추가된 4번째 의존성과 주석뿐인 `.gitignore` 규칙을 통과시켰고 `.git/`이 패키징 스캔에서 아예 빠져 있었다 — `requirements.txt` 실파싱과 `fnmatch` 패턴 매칭으로 바꾸고 `.git`을 패키징 제외 목록에 추가했다(단, `.gitignore` 대조 목록과는 분리 — `.git/`은 애초에 `.gitignore`에 적을 대상이 아니다). F-098 은 `where.py`가 단계 2c/7/8의 출구 대부분을 아예 실행하지 않아(2c=구식 바이너리 1개, 7=검증기 2건, 8=`board_verify.py` 1건) 완료 단계를 과대 판정할 수 있었다 — `CheckResult`를 3치(통과/실패/MANUAL)로 확장해 자동화 불가 항목(헤드리스 렌더·실측 로그 진위)을 "통과로 간주하지 않고" 명시적으로 남기는 구조로 바꿨다. F-101 은 `run_all.py`가 `spec_verify.py`를 인자 없이 돌리는 전체 회귀 경로에서 **F-063 이 막았다고 판정했던 결함이 그대로 재발**하는 지점이었다 — 검증기 기본 동작 자체를 비파괴(대조)로 뒤집어 근본 원인을 없앴다(재생성은 명시적 `--write`로만). F-100 은 GPT 프롬프트 문서의 "기존 발견 92건"·"F-001~F-096" 하드코딩이 `bug_fix_list.md`에 정상적으로 한 줄만 추가해도 매번 깨지는 구조였다 — 숫자를 아예 없애고 "그 자리에서 인덱스를 읽어라"는 동적 지시로 바꿨으며, 같은 파일의 "지난 19라운드"도 이번 처리 이력 추가로 즉시 재발할 것이 확실해 함께 고쳤다(F-100 이 지목한 것은 건수·ID 였지만 같은 결함 부류). F-099 는 아키텍처 §8.3 이 "`requirements.txt`가 전이 의존성까지 고정한다"고 적어 `CLAUDE.md` §4.3·실제 구현(직접 3개만)과 어긋난 문서불일치로, 판정 결과 문서가 틀려 정정했다 — `wheels/`가 패키지당 단일 버전만 담아 그 자체가 전이 의존성의 잠금 역할을 한다고 명시했다. F-097·F-098 은 `tools/*.py`가 `project_docs/**/*_verify.py` 명명 규칙 밖이라 기존 회귀 스캔·CP949 실행 루프 어느 쪽도 다루지 않던 사각지대였음도 드러나, `fix_log/meta_verify.py`에 §5-a(반례 8종 직접 재현)를 신설해 메웠다. 검증: `tools/offline_verify.py`(6조합 오프라인 설치 포함) 전체 통과 · `tools/where.py` 단계 1 판정 불변 · `tools/run_all.py` **10/10** · `python project_docs/dev/dev_verify.py` **76/76** · `python fix_log/meta_verify.py` 55 → **63/63** |
| 2026-08-07 | 화면·설계 전반 재검증 | F-083~F-090의 변경 파일을 재대조했다. 기능 2 주입 경로(F-084), 송신 빌더(F-086), 웹 검증기의 핵심 독립 입력(F-089)은 확인됐다. 그러나 표준 JSON Schema 검증에서 `AI_DRAFT + model_id:null`, `WIZARD + draft_text:null`, `selector.install_id:null` 등이 유효했고, DB에서도 `WIZARD + generation=AI`, 거부 사유 NULL·사후 변경이 허용됐다. NEC 알림의 `frame_id`도 필수가 아니었다. 웹·서비스 문서에는 A.1-5를 닫았다는 이전 판정과 쓰기 4→5·검증 76종이 남아 있었다. 그 상태로 DB 87/87 · 계약 56/56 · API 60/60 · 웹 55/55 · 서비스 37/37 · 메타 47/47가 모두 통과하여 F-091~F-095를 신규 기록했다. 웹 검증기에 `injectVector`·`Frame.fields`·`set_device_property`를 각각 제거하는 독립 주입은 모두 실패로 뒤집혀 F-089의 핵심 보완 자체는 확인했다. |
| 2026-08-07 | 계약 완결성 재검증 반영 | F-091 ~ F-095 (5건) 전량 수정, 기각 0. **넷이 한 형태다 — 직전 라운드가 `required`·`oneOf`·`minProperties` 를 넣고 '닫혔다'고 선언했지만, JSON Schema 의 `required` 는 키의 존재만 보고 `nullable` 값을 막지 않는다.** `{"origin":"AI_DRAFT","model_id":null}` · `{"install_id":null}` · NEC 알림의 `frame_id` 생략이 모두 유효했다. 분기마다 타입을 좁히고(`minLength`·`type:string`), 응답의 생성경로·거부 증거를 **nullable 필수**로 올렸다. DB 도 같은 부류였다 — `origin='WIZARD'` 인데 `generation='AI'` 인 행, 사유 없는 거부, 사유 사후 변경이 전부 통과했다. **F-095 가 이 라운드의 핵심**: 검증기가 `oneOf` 가 *있는지*만 보아 위 반례를 전부 60/60 으로 통과시켰다. 정상·반례 **43종 매트릭스**를 넣고, `jsonschema` 가 있으면 같은 매트릭스를 표준 구현으로 한 번 더 돌려 **판정 일치를 대조**한다(자체 검사기의 미구현 키워드가 통과로 바뀌는 것을 막는다). F-094 대응으로 메타 검증의 수치 대조를 문서 2개 → **`project_docs` 전체 20개**로 넓히고, 역사적 수치는 시점 표시로 구분한다. 검증: DB 87 → **98/98** · API 60 → **71/71** · 화면 55 → **62/62** · 서비스 37 → **42/42** · 메타 47 → **52/52**. 결함 주입 **19종**(API 스키마 9 · DB 제약 5 · 문서 5) 전량 검출 |
| 2026-08-07 | 화면·설계 전반 검증 반영 | F-083 ~ F-090 (8건) 전량 수정, 기각 0. **F-083·F-084 가 치명이며 둘 다 '화면 설계서가 선언한 사용자 흐름에 대응하는 HTTP 경로가 없다'는 한 형태다** — 설계서를 API 와 역추적해야만 보이는 종류로, 화면 단독 검토에서는 드러나지 않았다. F-083 은 초안 생성을 서버 실행으로 바꾸고(`RuleDraftRequest` 에서 `draft_text` 를 `if/then` 으로 금지) `generation` 열과 거부 영속 상태를 신설해 닫았다. **수정 도중 새 CHECK 가 `NULL IN (...)` 로 무효화되는 것을 발견해 함께 고쳤다 — F-039 와 같은 SQL 3치 논리다.** 같은 주입 과정에서 신규 제약 3건이 독립 반례를 갖지 못함이 드러나 제거했다(트리거 39 → **37**). F-089 는 F-080 의 재발로, `CLAUDE.md` §6.2 '독립 입력 대조' 규칙으로 승격했다. F-090 대응으로 메타 검증에 문서 ↔ `openapi.json` 수치 교차 검사(§4-b)를 신설했다. 검증: DB 76 → **87/87** · 계약 53 → **56/56** · API 40 → **60/60** · 화면 40 → **55/55** · 메타 45 → **47/47**. 결함 주입 20종 전량 검출 |
| 2026-08-07 | 화면·설계 전반 검증 | F-080~F-082 수정은 원문 포함 **39/39**로 재확인했다. 새 화면 설계를 API·FrameBuilder·표준 부속서까지 역추적하자 기능 3 AI 생성 경로(F-083), 기능 2 브라우저 주입 경로(F-084), 검증 뷰 응답 데이터(F-085)가 닫히지 않은 채 설계 완료로 선언된 것을 확인했다. 설정 API의 프로토콜 빌더·표준 판정·요청 스키마 공백(F-086~F-088), 이를 40/40으로 통과한 화면 검증기(F-089), 완료 단계 동기화 누락(F-090)을 추가 기록했다. 전체 기존 회귀는 모두 exit 0. |
| 2026-08-07 | 0937 재검증 반영 | F-080 ~ F-082 (3건) 전량 수정. **F-080 이 이 라운드의 핵심** — 신설 검증기가 대조표 한 파일만 읽어 "자기 자신과의 일치"만 검사했고, 문구 교환·가짜 근거 심벌·아키텍처의 F-079 역변경을 전부 23/23 으로 통과시켰다. 독립 입력을 넷(`0937_clauses.json` 신설 · 아키텍처 · `schema.sql` · `openapi.json`)으로 늘려 **37종**. F-081 은 §0 규약에 `"~가능해야 한다"` 매핑이 없어 강도 7건이 뒤틀린 것으로, 재분류 결과 `필수 16/능력 10/선택 7` → **`10/15/8`**. F-082 는 `✅` 를 '테이블이 있다'로 판정한 것이 원인이며, `zone` 컬럼이 실재하지 않는 등 근거 6건이 비어 있었다 — 4건은 `⚠` 로 낮추고 2건은 설계 계약(`mms.run_model` · `fms.check_stale_devices`)을 추가해 닫았다. 판정 `✅ 24/⚠ 3` → **`20/7`**. 결함 주입 10종 전량 검출 |
| 2026-08-07 | 0937 재검증 | **F-079 수정 자체는 표준 6.1/6.2 역할과 일치함을 확인.** 다만 신설 `services_verify.py`가 대조표 한 파일의 내부 형식만 검사하여 원문 문구 교환·존재하지 않는 구현 근거·아키텍처의 F-079 재발을 모두 통과하는 사각지대(F-080)를 확인했다. 원문 종결어미 독립 분류에서 강도 7건(F-081), 설계 산출물 역추적에서 필수 충족 판정 6건의 근거 공백(F-082)을 추가 확인했다. |
| 2026-08-07 | 0937 대조 | **GPT 지적이 아닌 자체 발견 (F-079).** 0937 요구사항 33건 + 부속서 A 31건을 조항 대 모듈로 1:1 늘어놓자, 아키텍처가 노드 속성 설정을 `dms`(0937 6.2 = 공공데이터 수집)에 배정한 것이 드러났다. 정답은 6.1 EMS 이며 `DEVICE_PROPERTY.Period` 는 6.1-2 '구동 주기 관리' 그 자체다. **3차~11차 검증이 전부 놓친 지점** — 문서 안에서는 자연스러워 보였고, 조항을 전수로 펼쳐야만 보였다. `services_verify.py` 신설 **23종** |
| 2026-08-05 | 11차 | F-076 ~ F-078 (3건) 전량 수정. **셋 다 '설계서 안은 맞는데 개발 정본 `CLAUDE.md` 가 반대로 적혀 있다'는 형태**다 — 10차에서 설계서만 고치고 정본을 함께 고치지 않은 결과다. Claude Code 는 `CLAUDE.md` 를 먼저 읽으므로 이 불일치가 설계서 내부 오류보다 구현에 더 직접 반영된다. **검증기가 `CLAUDE.md` 를 함께 읽도록 바꿨다.** F-077 은 정상 링크·기본 설정에서 `Period`(60s)와 `Keep Alive Interval`(60s)이 매번 동시 만료해 한쪽이 영구 기아하는 결함으로, `due` 비트 + 회전 커서 2 byte 로 해소했다. F-078 은 **gcc 13.3.0 으로 직접 컴파일해 세 주장을 실증**했다 — 반환형만 `bool` 로는 경고가 없고, `1u << 32` 는 검사 자체를 무력화한다. 마스크식으로 바꾸고 전 폭 스윕을 통과시켰다. 펌웨어 검증 37 → **50종**, 호스트 테스트 3 → **4종** |
| 2026-08-05 | 10차 | F-071 ~ F-075 (5건) 전량 수정. **F-071·F-072 가 무겁다** — 상태 머신이 표준의 Notify-ACK 재전송 흐름(8.2.1, 그림 8-45/8-46/8-56)을 통째로 빠뜨렸고, `FAULT` 가 연결 전·후를 겸해 **연결 승인을 우회하는 경로**가 있었다. 원문 그림 3장을 직접 판독해 확인했다. **F-074 가 이 라운드의 핵심 교훈** — 초안 검증기 26/26 은 상태 이름 존재와 '나가는 행 1개 이상'만 보아 필수 전이 삭제·요약 수치 변조를 전부 통과시켰다. 필수 전이 12종 대조와 요약표·계약 재산출을 넣어 **26 → 37종**. 재산출 로직 자체도 첫 구현에서 틀려(함수 인자의 `void *ctx` 를 멤버로 오인) 함께 고쳤다. 결함 주입 6종 전량 검출 |
| 2026-08-05 | 펌웨어 설계 | GPT 지적이 아닌 **설계 중 자체 발견**이다. 프레임을 바이트 스트림 위에 올리자 표준이 프레임 경계와 무결성 검사를 정의하지 않았음이 드러났다(**F-069 신규**, 총 19건). 스트리밍 디코드 채택으로 수신 버퍼가 501 → **51 byte** 가 되어 N 과 무관해졌다. 표준 미규정 4건을 추가 결정. `firmware_verify.py` 신설 **26종** |
| 2026-08-03 | 1차 | F-013 ~ F-017, F-022 (6건) 전량 수정. DB 제약 테스트 32 → 56종, 계약 테스트 29 → 36종 |
| 2026-08-03 | 2차 | F-023 ~ F-033 (11건) 전량 수정. **F-027(회신 경로 부재)이 치명** — 문서대로 구현했으면 기능 1이 동작하지 않았다. 표준결함 5건 신규 확정(F-034~F-038)으로 총 17건. DB 제약 테스트 56 → **61종**, 계약 테스트 36 → **37종** |
| 2026-08-03 | 3차 | F-039 ~ F-044 (6건) 전량 수정. **F-039(승인 스냅샷 우회)가 치명** — 승인 게이트가 내용 수준에서 다시 뚫려 있었다. F-043 대응으로 `fix_log/meta_verify.py` 신설, 수치 대조를 자동화. DB 제약 테스트 61 → **67종**, 계약 테스트 37 → **46종**, SIAP 명세 검증에 4차(Value 범위·왕복) 15종 신설 |
| 2026-08-05 | 9차 | F-066 ~ F-068 (3건) 전량 수정. **F-068 은 지적된 B03 외에 3건이 더 있었다** — `NP_3DEV` 상수를 재사용해 `Num. of Devices` 와 실제 개수가 어긋난 벡터가 4건, 자기모순인 B01 까지 5건. 표 7-16 불변식을 생성기와 검증기 양쪽에 넣었다. **독립 인코더 교차 검증이 이 수정을 잡아냈다** — 골든만 고치자 `spec_verify` 예시와 바이트가 어긋났다. F-066 은 커밋 누락이며 `--check` 가 의도대로 드러낸 사례다. 골든 검증 28 → **29종** |
| 2026-08-05 | 8차 | F-060 ~ F-064 (5건) 전량 수정. 골든 벡터 단계의 첫 검증이다. **F-060 이 가장 무겁다** — 정상 NEC 알림을 `violations` 에 넣어 두면 `ingest.handle()` 이 격리 후 반환해 alert 저장과 ACK 가 사라진다. `judgement` 로 위반/알림을 분리했다. F-063 대응으로 생성기 2종에 `--check` 를 넣어 검증 중 산출물 덮어쓰기를 막았다. **표준결함 F-065 신규**(Timeout ↔ N 상한 미규정)로 총 18건. 골든 검증 21 → **28종** |
| 2026-08-03 | 7차 | F-058 · F-059 (2건) 전량 수정. **둘 다 6차 수정의 빈틈이다** — F-055 가 만든 '단일 예외' 계약이 변환 단계에서 깨졌고, F-057 이 만든 '정확히 하나' 불변식이 누락만 검사하고 중복은 놓쳤다. 지적된 FLOAT 경로뿐 아니라 **정수 경로의 같은 누출도 함께** 고쳤다. SIAP 4차 23 → **29종**, API 반례 13 → **17종**, 메타 27 → **30종** |
| 2026-08-03 | 6차 | F-055 ~ F-057 (3건) 전량 수정. **셋 다 직전 라운드 수정의 빈틈이다** — F-054 의 반례 집합에 FLOAT 가 없었고, F-053 의 소유권 표가 테이블 9개를 덮지 못했으며, 쓰기 경로 검사는 개수만 셌다. 검사 방식뿐 아니라 **반례 집합의 완결성**과 **표의 완결성**을 각각 자동 검사로 고정했다. API 검증 34 → **35종**, SIAP 4차 18 → **23종**, 메타 25 → **27종** |
| 2026-08-03 | 5차 | F-050 ~ F-054 (5건) 전량 수정. **F-050 이 가장 무겁다** — 4차에서 만든 CP949 검사가 Windows 에서 조용히 무효화되는 구조였다. 자식 출력을 bytes 로 받아 디코딩 실패를 FAIL 로 만들고, 결함 주입으로 검출을 재확인했다. F-053 으로 아키텍처의 DB 쓰기 소유권을 테이블 단위로 재정의. API 검증 26 → **34종**, 메타 검증 출력 길이 표기 추가 |
| 2026-08-03 | 4차 | F-045 ~ F-049 (5건) 전량 수정. **F-049(승인 대상 미결속)가 치명** — 승인한 명령을 그대로 두고 다른 구동기를 켤 수 있었다. 승인 스냅샷에 `target_install_id` 를 추가해 '어느 장치를' 까지 DB가 강제한다. F-045 대응으로 `meta_verify.py` 에 CP949 콘솔 출력 회귀 검사 신설. DB 제약 테스트 67 → **76종**, 계약 테스트 46 → **53종**, SIAP 4차 검증 15 → **18종**, 메타 검증 14 → **20종** |
