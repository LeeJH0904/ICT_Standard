# F-249 · 환경 측정 서브타입 설명 주석이 10종을 9종으로 적는다

| 항목 | 값 |
|---|---|
| 심각도 | 위험 |
| 분류 | 문서불일치 |
| 대상 | 제출본 `project_code/backend/schema.sql:288-300` · 1369-Part1 6.3.3 |
| 발견일 | 2026-08-18 |
| 상태 | 수정완료 |

## 근거

TTAK.KO-10.1369-Part1 6.3.3.1~6.3.3.11은 환경상태 데이터로 온도, 습도, 일사,
이산화탄소, 풍향, 풍속, 토양수분장력, 전기전도도, 수소이온농도, 감우 10종을 설명한다.
표준 자체의 그림 누락 문제는 `F-011` 표준결함으로 이미 이관됐다.

## 현상

`env_measurement`의 CHECK는 위 10종을 정확히 열거하지만 바로 앞 주석은
"그림 7-3의 9개 서브타입", "8종은 공통 구조"라고 적는다. 실제 CHECK 기준으로는
감우를 제외한 9종이 공통 구조다.

## 영향

실행 스키마는 올바르므로 현재 데이터 저장은 깨지지 않는다. 다만 표준 적용 범위를
코드 주석으로 추적하는 심사자와 유지보수자가 9종 구현으로 오인할 수 있다.

## 제안

본문 우선 결정에 맞춰 주석을 "10개 서브타입", "9종 공통 구조 + 감우"로 정정하고
`F-011`의 표준 그림 누락과 구분해 적는다.

---

## 작업자 처리 기록

| 일시 | 상태 | 내용 |
|---|---|---|
| 2026-08-18 | 확인 | **판정**: 표준 1369-P1 6.3.3.1~6.3.3.11은 환경상태 10종(감우 포함)을 규정하고 `env_measurement`의 CHECK도 10종을 정확히 열거함. 주석만 "9개/8종"으로 표기 → **주석이 틀렸다**(CHECK·표준이 정본). 표준 그림 누락(F-011 표준결함)과는 별개 |
| 2026-08-18 | 수정완료 | `backend/schema.sql`의 `env_measurement` 앞 주석을 "6.3.3의 10개 서브타입 통합 / 9종은 (측정값·단위·오차범위·유효범위) 구조 동일, 감우만 측정값(+단위) 단독"으로 정정. CHECK 본문·DDL은 이미 옳아 변경 없음(실행 스키마 무영향) |
| 2026-08-18 | 수정완료 | **적용 범위**: 제출본(`최종_제출물_폴더/ICT_Test/project_code/backend/schema.sql`)에 적용. 이후 사용자 지시로 **개발 트리 `project_code/backend/schema.sql`** 및 **설계 정본 `project_docs/db/schema.sql`** 에도 동일 정정 — 정본을 함께 고쳐야 다음 동기화 때 오류가 재전파되지 않는다(CLAUDE.md: `project_docs/db/schema.sql`이 정본, `backend/schema.sql`은 동기 사본). CHECK/DDL은 세 곳 모두 이미 옳아 변경 없음(주석만 정정) |
