# 제출본 주석 정리 관례 — 남은 60개 파일 설계문서·§ 참조 제거

> 대상: `최종_제출물_폴더/ICT_Test/project_code/` 의 **코드 파일 60개**.
> 블라인드 심사용으로, 코드는 그대로 두고 **주석·독스트링·테스트이름 문자열에 남은
> 내부 설계문서 참조만** 제거한다.

---

## 0. 전제 (이미 끝난 것 — 다시 하지 않는다)

- **F-번호(fix_log 버그 ID)는 전 코드 트리에서 이미 제거·검증 완료.** 이 문서는 그 다음
  단계인 **설계문서·§ 참조 제거만** 다룬다.
- 완전히 끝난 파일(F-번호 + 문서참조 모두): `web/**`, `contracts/**`, `siap/codec.py`.
  → 이 파일들은 건드리지 않는다.

---

## 1. 핵심 원칙 3줄

1. **코드는 한 글자도 바꾸지 않는다.** 주석·독스트링·문자열만 손댄다.
   (검증으로 **코드 토큰이 원본과 동일**함을 매번 확인 — §5)
2. **표준 참조·실제 파일 경로는 남긴다.** 지우는 건 *우리 내부 설계문서* 참조뿐이다.
3. **지워서 문장이 깨지면 뜻을 살려 다시 쓴다.** 한국어 조사(`의·를·에·와`)·괄호가
   덜렁 남지 않게 리워딩한다 — 단순 삭제로 `( )`·`— `·`, `가 남으면 안 된다.

---

## 2. 제거 대상 (남은 60개 파일 실측)

| 마커 | 건수 | 처리 |
|---|---|---|
| 바레 `§X.Y` (예: `§5.3`, `§5.1/§5.7`) | **513** | 삭제. 대부분 문서명 뒤나 괄호 안에 붙어 있다 |
| `CLAUDE.md §X` | **113** | 삭제. 필요하면 그 조항의 취지를 한 마디로 풀어씀 |
| `아키텍처(_설계서) §X` | **57** | 삭제 (※ 일반어 "아키텍처"는 남긴다 — 문서명일 때만) |
| `펌웨어 설계서 §X` | **49** | 삭제 (※ 일반어 "펌웨어"는 남긴다) |
| `시연(_시나리오) §X` | **12** | 삭제. `X01~X08`·`Node ID=3` 등 도메인 값은 남긴다 |
| `Frame 구조 명세서 §X` | **12** | 삭제 |
| `대조표` (0937 요구사항 대조표 등) | **11** | 삭제 |
| `(SIAP )메시지 명세서 §X` | **10** | 삭제 |
| `tools/*.py` (미배포 검증기 경로) | **10** | 삭제 또는 "검증기가 …" 로 일반화 |
| `project_docs/*` (미배포 설계 트리) | **5** | 삭제 |
| `개발_착수(_지시서)`·`개발_운영`·`검증자_*` | **5** | 삭제 |
| `openapi.json …` | **4** | 삭제 (규격명만 남김, 예: `… — ISO 8601`) |
| `골든벡터_명세서` | **2** | 삭제 (※ 도메인어 "골든벡터"·"골든 B03"은 남긴다) |
| `화면_설계서(.md) §X` | **1** | 삭제 |

> 정리 후 위 마커가 **0에 수렴**해야 한다.

---

## 3. 남기는 것 (지우지 말 것)

- **표준 참조**: `TTAK.KO-10.0943/0937`, `1369-P1`, `표 7-14`, `절 7.3.1`, `8.1.3.2`,
  `WCAG 2.1 AA`, `RFC 9457`, `IEEE-754`, `표준 8장` — 심사 자산이다.
- **실제 배포되는 파일 경로**: `contracts/frame.py`, `firmware/core/bitpack.c`,
  `siap/codec.py`, `sim/inject.py`, `test_contract.py` 등 — 제출물 안에 실재하므로 유지.
- **코드·식별자·문자열 로직**: `RSC.INVALID_FORMAT`, `clause="7.3.1"` 같은 **값**은 절대 불가.
- **도메인 용어**: `골든벡터`/`골든 B03`(개념), `X01~X08`(주입 시나리오), `T_gap`,
  `Node ID=3` 등.
- **표준 조항 번호 주석**: `/* 표 7-14: DEVICE_MAIN_INFO — 56 bit */` 유지.
- **테스트 함수명의 표준 조항 번호**: `test_invalid_format_7_3_1` 유지.

즉 **"우리 내부(설계문서·우리 도구·개발 단계)" 참조만 제거하고 "표준·배포 코드" 참조는 보존**.

---

## 4. 워크드 예시 (리워딩으로 뜻 보존)

```
- (CLAUDE.md §4.4 "C 를 먼저 쓰고 Python 으로 옮긴다" — 반대 방향은 AVR 제약을 놓친다)
+ ("C 를 먼저 쓰고 Python 으로 옮긴다" — 반대 방향은 AVR 제약을 놓친다)
  → 문서명·§만 떼고 인용은 남김

- 분기 구조를 C 원본과 줄 단위로 대응시킨다(펌웨어 설계서 §5.3) — 여기서
+ 분기 구조를 C 원본과 줄 단위로 대응시킨다 — 여기서
  → 괄호 통째 삭제

- 결함 주입 지점 순서는 펌웨어 설계서 §5.4 표를 그대로 따른다:
+ 결함 주입 지점 순서는 아래 표를 그대로 따른다:
  → 문서 참조를 "아래 표"로 일반화(뜻 보존)

- 한다(tools/xcodec_verify.py)."""
+ 한다."""
  → 미배포 도구 경로 삭제

- 예외를 던지지 않는다(Frame 구조 명세서 §3 계약).
+ 예외를 던지지 않는다.
  → 괄호 통째 삭제
```

**하지 말 것**: `(계약)`처럼 알맹이 없는 괄호만 남기기, `데 §5.7 문서의` → `데 문서의`
처럼 조사 꼬임 방치. 지운 뒤 문장을 읽어 자연스럽게 다듬는다.

---

## 5. 검증 (파일마다)

1. **코드 토큰 동일성** — `.py`는 원본(HEAD)과 *주석·문자열을 뺀* 토큰이 완전히 같아야 한다.
   `tokenize` 로 COMMENT·STRING 토큰을 걸러내고 나머지 토큰열을 비교 → 다르면 로직을
   건드린 것이므로 즉시 되돌린다. (이 방식으로 F-번호 정리 시 로직 무변경을 증명했다.)
2. **컴파일·테스트** — `python -m py_compile`, 그리고 해당 계층 테스트:
   `python -m pytest siap/tests backend/tests sim/tests`, `contracts/test_contract.py`,
   펌웨어는 `firmware/tests && make && ./test_*`. 주석만 바꿨으니 전량 그대로 통과해야 한다.
3. **마커 스캔 0** — §2 표의 마커를 grep 해 0 확인(실파일 경로·표준 참조는 오탐이니 제외).

---

## 6. 남은 60개 파일 (디렉터리별) · 권장 순서

심사자가 먼저 읽는 **소스 → 그다음 테스트** 순으로, 디렉터리 단위로 처리하고 매번 §5 검증.

| 디렉터리 | 파일 수 |
|---|---|
| `siap/` (codec.py 제외) | 5 |
| `sim/` | 5 |
| `backend/` | 5 |
| `backend/services/` | 6 |
| `run.py` | 1 |
| `firmware/core/` | 8 |
| `firmware/arduino_sensor_node/` | 3 |
| `firmware/arduino_actuator_node/` | 3 |
| `firmware/esp32_node/` | 2 |
| `sim/tests/` | 4 |
| `siap/tests/` | 3 |
| `backend/tests/` | 8 |
| `firmware/tests/` | 7 |
| **합계** | **60** |

정확한 파일 목록은 아래 명령으로 재생성한다(실파일 경로·표준 참조 오탐 감안해 눈으로 확인):

```
cd 최종_제출물_폴더/ICT_Test/project_code
grep -rlE "화면_설계서|아키텍처 설계서|펌웨어 설계서|Frame 구조 명세서|메시지 명세서|\
DB_?스키마 설계서|골든벡터_|API_?명세서|CLAUDE\.md|대조표|_verify\.py|시연 시나리오|\
§[0-9]|tools/|openapi\.json|project_docs|개발_착수|개발_운영|검증자_" \
--include="*.py" --include="*.c" --include="*.h" --include="*.ino" \
| grep -vE "web/|contracts/|siap/codec\.py"
```

---

## 7. 주의 — 별도로 남겨 둔 데이터 파일

`contracts/vectors/golden.jsonl`·`golden_ext.jsonl` 의 `note`/`clause` 필드에도
F-번호·`CLAUDE.md` 참조가 있다. 이건 **코드 주석이 아니라 생성된 데이터 파일**(직접 편집
금지 규칙 대상)이라 위 절차와 분리한다. 손대려면 hex·fields 를 바꾸지 않도록 `note`/상위
`clause` 텍스트만 정리하고, 골든 검증을 다시 돌려 바이트 불변을 확인한다.
