# 이 파일을 .env로 복사해 값을 입력한다. .env에는 실제 키가 있으므로 커밋하지 않는다.
# 같은 이름의 프로세스 환경변수가 있으면 그 값이 .env보다 우선한다.
KMA_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
OPENAI_TIMEOUT_SEC=8
